# TamashaRoom — Project Tasks

## Completed

### Video proxy: follow redirects on stream open + real 206 headers + probe retry (2026-08-13)

**Batch scope:** two production video-player errors against the proxy path (`PipelineStatus::PIPELINE_ERROR_READ: data source error` at load/play; `MEDIA_ELEMENT_ERROR: Format error` mid-playback after a few minutes plus repeated seeks). Root causes confirmed in `app/Services/VideoProxyService.php`:
1. **Redirect on stream open not followed.** `openRemoteStream()` opens the probe-resolved final URL with `follow_location=0`; an edge CDN that rotates nodes between `fetchHead` and the actual range GET (verified against the failing room 903 source, a 3-hop abrtech → edgevurq7 → edge11 chain) answers the stream open with a 3xx. `handleRangeRequest` only special-cased `upstreamStatusIs200()`, so the redirect stub body (empty/HTML) was relayed as a synthesized 206 → `Format error` at load or after a seek.
2. **206 `Content-Length`/`Content-Range` computed from the probe, not the actual upstream response** → declared-vs-actual byte mismatch on seek.
3. **Non-2xx (403/404/416) on stream open was relayed as video bytes** instead of an honest error.
4. **Intermittent `fetchHead` probe failure → 502 `Failed to reach video source`** (log-confirmed `00:29:55` for room 903) → load-time data-source error.

- [x] **`VideoProxyService` fix.** New `openStreamFollowingRedirects()` follows redirects on the actual stream open (bounded by `MAX_REDIRECTS`, per-hop SSRF validation, visited-set — mirroring `fetchHead`), so an edge rotation between probe and open is resolved instead of relayed. `handleRangeRequest`/`handleFullRequest` now prefer the upstream's real `Content-Range`/`Content-Length` for the 206 and return a clean 502 (empty `video/mp4`, never JSON) for any non-2xx status. `stream()` retries `fetchHead` once to absorb transient probe failures. Removed the now-dead `upstreamStatusIs200()`; `openRemoteStream` now also captures `$http_response_header`.
- [x] **Tests.** `tests/Feature/VideoStreamTest.php`: 4 new regression tests (redirect followed on range open; redirect followed on full open; non-2xx → 502; actual upstream `Content-Range` used for the 206). One existing full-stream stub now sets `lastStreamStatus = 200` to match the real `openRemoteStream`.
- [x] **E2E flake (pre-existing, unrelated).** `tests/e2e/room.spec.ts:67` `waitForLoadState("networkidle")` never settles once the guest streams the room's video — reproduced identically on the pre-change code (2 consecutive failures, both original and fixed). Replaced with the project's established `waitForSelector("video")` pattern (see `playback-sync-verification.spec.ts`). The chat.spec tab-switch timeout and the 2 contrast-a11y strict-locator failures (`getByRole('button', { name: 'زیرنویس' })` substring-matching both "زیرنویس" and "تنظیمات زیرنویس") also reproduce identically on unmodified code — pre-existing timing/UI-locator flakes, unrelated to the proxy.
- [x] **Verification.** `php artisan test` **278/278** (2096 assertions, incl. 4 new proxy tests); `npm run test` **235/235**; `npm run type-check` clean; ESLint clean; Pint clean; full `npm run test:e2e` **24/24** (3.0m); a11y **17/19** (2 pre-existing contrast failures reproduced on unmodified code). Nothing committed/pushed.

### Multi-tab session preservation & presence reconnection fixes (2026-08-12)

**Batch scope:** resolved two UX bug reports regarding multi-tab session handling and tab close/reopen room membership without weakening session security.
1. **Multi-Tab Session Security Fix (Bug 1):** Reverted backend `$session->migrate(false)` back to secure `$destroy = true` defaults in `AuthenticatedSessionController`, `RegisteredUserController`, and `RoomController::join` to close the session fixation vulnerability. Division of responsibilities:
   - **Cross-Tab Session Protection (Backend):** `SuppressAnonymousSessionCookie` middleware (prepended to `web` group) strips unauthenticated `Set-Cookie` headers on background API/XHR polling routes (`playback.*`, `presence.*`, `chat.*`, `proxy.*`). When Tab 2 logs in and regenerates the session ID, Tab 1's trailing background poll fails auth (401), but the middleware prevents a guest `Set-Cookie` from reaching the browser, leaving Tab 2's authenticated session cookie intact in the shared cookie jar.
   - **Same-Tab Navigation Protection (Frontend):** `polling-controller.ts` & Axios interceptor in `api.ts` (checked by `usePlaybackSync`, `usePresence`, `RoomChat`, and Inertia navigation events) pause/cancel background polling before session-regenerating actions within the same browser tab.
2. **Presence & Reconnection Fix (Bug 2):** Root cause confirmed as `use-presence.ts` `applySnapshot` firing `onRemoved()` (which redirected to `/dashboard` with "شما از اتاق حذف شده‌اید.") whenever a user's ID was absent from a presence list snapshot (e.g. Pusher presence channel `channel.here` which lists socket-connected users only). **Fix:** Removed the snapshot absence check from `use-presence.ts` so `onRemoved()` only triggers on explicit HTTP 403/404 server authorization errors (confirming database removal via owner kick). Room membership in `room_members` remains intact when tabs close (`presence_status = 'offline'`); reopening the tab or room URL directly seamlessly reconnects the member, restores presence to `'online'`, and syncs playback.
- [x] **Backend Fix.** Reverted to secure `$destroy = true` session regeneration; added `SuppressAnonymousSessionCookie` middleware prepended to `web` group (cross-tab protection).
- [x] **Frontend Fix.** Added `polling-controller.ts` interceptor, `isPollingSuspended()` checks in polling hooks, Inertia router event hooks (same-tab mitigation), and removed snapshot `myId` check in `use-presence.ts`.
- [x] **Tests.** Added PHPUnit `MultiTabSessionTest.php` (3 tests, including session fixation prevention & cookie suppression) and Playwright E2E `multi-tab-reconnect.spec.ts` (2 tests).
- [x] **Verification.** `php artisan test` **270/270** (2061 assertions); `npm run test` **234/234**; `npm run type-check` clean; ESLint clean; Pint clean.

### E2E room.spec "Playback state propagates" fails in Pusher mode: fake-URL revert race (2026-08-12)

**Batch scope:** the E2E test "Playback state propagates from host to guest" (`tests/e2e/room.spec.ts:45`) failed locally under `BROADCAST_CONNECTION=pusher` (18/22) while passing in CI. Root cause is a test-design bug, not an app defect: the test set the room's video to an unplayable fake URL (`https://www.example.com/video.mp4`) via `set-video`, then PATCHed `is_playing:true` via the API. Under Push transport with a running queue worker (the E2E `globalSetup` drains the DB queue), the `PlaybackStateChanged` broadcast now actually reaches the host's live room page, whose player fails to load the fake URL and fires a native `pause` — `SyncedVideoJsPlayer.handlePause` (host = `canControl`) then PATCHes `is_playing:false` back, reverting the room (`state_version` went 3→4 in the DB) before the guest's first poll. CI never saw this because `.env.example` uses `BROADCAST_CONNECTION=null`, making `broadcast()` a no-op the host page never receives. The passing playback-sync/tap-to-play suites all use the real playable `local_video` fixture for exactly this reason.

- [x] **Test-side fix.** `tests/e2e/room.spec.ts` now creates the room with `?local_video=1` (real same-origin `sample.mp4`, playback_mode=direct) and asserts the guest sees the helper's actual `video_url` instead of a hardcoded fake URL. The separate `set-video` API call was dropped (the fixture sets the URL at creation; set-video coverage is owned by `PlaybackStateChanged`/sync suites). `routes/test-helpers.php` `setup-verified-room` now returns `video_url` in its JSON so the test can assert against the real fixture. The app's sync transport/contract is untouched.
- [x] **Root cause proof.** A throwaway spec confirmed the mechanism: with the fake URL the state reverted; with a real playable video the same PATCH stuck and the guest saw `is_playing:true` (temp spec deleted after use).
- [x] **Verification.** Full `npm run test:e2e` **22/22** (3.0m) with the worker running under Pusher; `php artisan test` **267/267** (2042 assertions); Pint clean; `npm run build` clean.
- [x] **Docs.** This entry added to `docs/TASK.md`. Nothing committed/pushed.

### CI room-nav 302s to /login: stale-session-cookie overwrite race in the room setup flow (2026-08-12)

**Batch scope:** the contrast-a11y room tests ("Room open states", "Room dialogs") could still 302 to /login intermittently in CI after the 08-11 timeout batch. Evidence from response/cookie/session-DB logging shows the root cause is a test-flow artifact, not an app defect: the setup route logs the test user in, and `Auth::login()` regenerates the session ID; an in-flight room-page poll (playback-sync/presence) carrying the pre-regeneration ID can be processed after that regeneration and resurrect the stale ID as an empty guest session, and that response's Set-Cookie then overwrites the browser's fresh authenticated session cookie — so the next room navigation is redirected to /login. A single re-run of the setup-then-navigate flow always landed (confirmed across the logged runs). The app's transport design is untouched; the race lives entirely in the test's navigation pattern.

- [x] **Test-side fix.** New shared `tests/a11y/room-nav.ts` exports one race-safe `gotoRoom(page, params)`, and **all six** room-navigating a11y tests route through it — the five contrast tests plus `room-a11y.spec.ts` (which had the same single-room-nav pattern): if the room navigation lands on `/login`, it re-runs the setup (fresh login + session regeneration replaces the stale cookie) and navigates to the new room. Because no room page is alive during the retry, no racing poll can repeat the overwrite — the retry is deterministic, so a single retry (not a while-loop) is used.
- [x] **Temporary diagnostics removed.** `tests/a11y/contrast-a11y.spec.ts`: the `dumpRoomPageState` / `sessionCookieInfo` / `captureRoomNavFlow` helpers and the `waitForRoom` catch-wrapper are gone (`waitForRoom` moved into `room-nav.ts` as the plain tab-bar wait; the formerly unused `Response` import was dropped). Server side: `app/Http/Middleware/SessionDiagMiddleware.php` deleted, its registration removed from `bootstrap/app.php`, and the `SESSION-DIAG` log blocks in `routes/test-helpers.php` and `RoomController::show` removed.
- [x] **Verification.** Full a11y suite **19/19** (53.5s) with the helper now shared; contrast spec **8/8** twice consecutively (32.7s / 32.5s); `tsc --noEmit` clean; ESLint clean; `php artisan test` **267/267**; Pint clean and `php -l` clean on all three touched PHP files. Test counts unchanged (diagnostics-only removal + shared-helper refactor).
- [x] **Docs.** This entry added to `docs/TASK.md`. Nothing committed/pushed.

### Room-request diagnostics: playback-sync + video-proxy failure observability (2026-08-11)

**Batch scope:** structured observability for room-scoped endpoints (playback sync + video proxy) so every failure is grep-able in production and, when a DSN is set, correlated in Sentry as an HTTP breadcrumb. Backend-only; no transport/sync-contract changes.

- [x] **New `RoomRequestDiagnosticsService`** (`app/Services/RoomRequestDiagnosticsService.php`): maps route names to a category (`playback` / `proxy`; `null` for non-target routes) and records each failure as one `[diagnostics:{category}]` log line with full context — endpoint/method/path, `http_status`, `room_id`, `user_id`, timestamp, `rate_limited`, `failure_type` (429→rate_limit, 403→forbidden, 404→not_found, 422→validation, 502/504→upstream, other 5xx→server_error, else client_error), plus limiter name/limit/current-count/retry-after for 429s (cache key mirrors `ThrottleRequests` exactly). **5xx logs via `Log::error`; lower-severity failures (429/403/404/422) via `Log::warning`** — so the proxy's upstream 502 and any other 5xx are error-severity.
- [x] **New `DiagnoseRoomRequest` middleware** (`app/Http/Middleware/`): records every rendered `>= 400` response on the routes it guards. Registered per-route on `playback.*` (category `playback`) and `proxy.video` (category `proxy`).
- [x] **Middleware-order correctness for the `{room}` binding.** As route-level middleware, `DiagnoseRoomRequest` runs after the `web` group's `SubstituteBindings` (group middleware run outer), so `$request->route('room')` resolves to the bound `Room` model — not the raw id — in every recorded context.
- [x] **Exception-driven failures (most importantly the throttle 429) recorded in `bootstrap/app.php`.** The throttle's `ThrottleRequestsException` is thrown OUTER to `DiagnoseRoomRequest`, so it never surfaces as a `>= 400` response to the middleware. A new `$exceptions->render` callback (registered before the generic debug JSON renderer) matches target routes, derives status/failure type from the exception (`ThrottleRequestsException`→429 + the category's named limiter, `ValidationException`→422, `HttpExceptionInterface`→its code, else 500), records it, and returns `null` so Laravel's own rendering is never altered. The two paths (in-middleware responses vs exception-driven) never double-record.
- [x] **Tests.** New `tests/Feature/PlaybackDiagnosticsTest.php` (5 tests): playback 429 records with limiter limit/count/name; playback validation 422 records; proxy 404 records; proxy upstream 502 records via `Log::error` (not `warning`); proxy 429 records with limiter context. Both the middleware path and the exception path are covered via Log-spy assertions.
- [x] **Verification.** `php artisan test` **267/267** (was 262; +5 new); Pint clean. Vitest, a11y, and E2E unaffected (backend-only change).
- [x] **Docs.** This entry added to `docs/TASK.md`.

### Room-player video bugs: stale `video_url` reverts + host never reconciles after set-video (2026-08-11)

**Batch scope:** two confirmed root causes behind "replacing the video mid-playback keeps the old one" and "the submitted video never appears". Evidence-based; each fixed with a failing test first. Nothing committed/pushed.

- [x] **Root cause #1 (replace keeps old video): routine position syncs carried the host's stale `video_url`.** `use-playback-sync`'s `sync()` always sent `video_url: prev.videoUrl` on every host PATCH (`use-playback-sync.ts:252`), so after `set-video` bumped the room to the new URL the host's **next** play/pause/seek/timeupdate PATCH sent its old local URL. `PlaybackController::update` legitimately honors a PATCHed `video_url` (documented by `test_host_can_update_video_url_via_sync`), so the server "reverted" the just-set video — deterministically whenever the host acted before a poll/broadcast caught it up, and permanently in push mode (see #2). **Fix:** `sync()` now omits `video_url` unless the caller explicitly passes it (spread-gated payload); only `set-video` and intentional URL syncs change the video.
- [x] **Root cause #2 (video never appears): the host never reconciled its sync state after set-video.** `Show.tsx` posted `set-video` and ignored the response; `use-playback-sync` only re-seeds on mount/visibility/socket-reconnect. In push mode the host (and every guest downstream of it) waited on the queued `PlaybackStateChanged` broadcast, which on `QUEUE_CONNECTION=database` rides the cron `queue:work --stop-when-empty --max-time=30` drain (≤~60s, or never locally without a worker) — the driver for "sometimes never appears". **Fix:** new optional `refreshKey` option on `use-playback-sync` forces an authoritative `GET /playback/{room}/state` when it changes (deduplicated, so an unchanged key does not refetch); threaded through `SyncedVideoJsPlayer` and bumped in `Show.tsx` on `set-video` success, so the host reloads the new video immediately and its own subsequent broadcasts propagate it.
- [x] **Regression tests (3 new, all failing before the fix):** "omits `video_url` from a routine position sync"; "refetches authoritative state when `refreshKey` changes"; "does not refetch when `refreshKey` is unchanged" (`resources/js/__tests__/use-playback-sync.test.tsx`).
- [x] **Verification.** Vitest **234/234**; `npm run lint` (ESLint) and `npm run type-check` (`tsc --noEmit`) clean; `php artisan test` **262/262** (1942 assertions) — the documented PATCH-can-change-video contract is untouched; Pint clean.
- [x] **Docs.** This entry added to `docs/TASK.md`.

### CI contrast-suite timeouts: clipboard permission (toast) + scoped budget for the 8-scan dialogs test (2026-08-11)

**Batch scope:** two `contrast-a11y.spec.ts` tests ("Room open states (subtitle manager + toast)" and "Room dialogs") timed out in CI's bundled headless Chromium at the 30s default budget while passing locally on system Chrome. Root-caused both: a deterministic Clipboard-API permission gap for the invite-link copy toast, and a cumulative-load budget issue (not a wait-logic bug) in the heavier dialogs test.

- [x] **Root cause #1 (toast test): missing `clipboard-write` permission in CI's bundled headless Chromium.** The copy button calls `navigator.clipboard.writeText` via `safeCopyToClipboard` (`resources/js/lib/utils.ts`). Bundled headless Chromium (CI runs `npx playwright install chromium`) does **not** auto-grant the Clipboard API; system Chrome does (secure context `127.0.0.1` + user gesture), which is why the test passed locally and timed out only in CI. On rejection the app shows the *error* toast (`کپی شدن لینک ممکن نشد. …`) so the wait on `لینک دعوت کپی شد.` never resolves. Confirmed the mechanism with a probe: native copy → success toast; forced-reject → error toast, success toast never appears. **Fix:** `permissions: ["clipboard-read", "clipboard-write"]` added to the `use` block of `tests/a11y/playwright.config.ts` (CI's config) and the machine-local Chrome/Edge fallback configs.
- [x] **Root cause #2 (dialogs test): cumulative wall-clock, not the چت wait.** Verified the previous networkidle→`waitForRoom` hardening (commit `66d718b`) **is** present in this test's own `gotoRoom()` — no wait-logic gap. The test performs **8 full-page `color-contrast` scans across 4 dialog states × both themes plus a fresh room setup per theme** (~10s locally). CI's single-core bundled headless Chromium pushes the whole test past 30s, so the چت wait in the second `gotoRoom()` runs out of clock. **Fix:** scoped `test.setTimeout(60_000)` on this one test only (with an explaining comment) — not retries, not a blanket config timeout, and no lost scan coverage.
- [x] **Verification.** Both failing tests pass over the CI-style HTTP fallback (`PLAYWRIGHT_BASE_URL=http://127.0.0.1:8000`, system Chrome): toast 8.2s / dialogs 7.8s. Full a11y suite **19/19** twice consecutively (~1.1m per run). Bundled-Chromium exactness remains unverifiable locally (CDN blocked; documented in ISSUE_REGISTER), but the permission grant is the documented Playwright requirement for headless Chromium clipboard use.
- [x] **Docs.** This entry added to `docs/TASK.md`. Change not yet committed/pushed.

### Herd HTTPS base-URL priority for Playwright + machine-local Edge fallback (2026-08-11)

**Batch scope:** Playwright suites now prefer the local Herd HTTPS domain (`https://tamasharoom.test`) as base URL when reachable, falling back to the `php artisan serve` dev server; added a never-committed system-Edge fallback config; hardened the one a11y test that depended on the webServer-only `MAIL_MAILER=array`.

- [x] **New `tests/playwright-base-url.mjs` helper** shared by all four configs. Priority: `PLAYWRIGHT_BASE_URL` env var (explicit override) → Herd domain if a quick reachability probe answers on 443 → `http://127.0.0.1:8000` fallback (matches every config's `webServer` command). Herd is preferred because it terminates **real HTTPS** via its own local CA — so `SESSION_SECURE_COOKIE` and other HTTPS-only behavior are exercised in tests instead of being silently bypassed on plain HTTP. The probe intentionally skips TLS validation (Node's cert bundle doesn't trust Herd's CA; the browser under test does).
- [x] **Wired into `tests/a11y/playwright.config.ts`, `tests/e2e/playwright.config.ts`, and the two machine-local configs** via `const baseURL = await resolveBaseUrl()`. Added `ignoreHTTPSErrors: true` to every config's `use` block: `page.request` runs on Node's TLS stack, which does not trust Herd's CA, so `__test` helper calls would otherwise fail over the HTTPS base URL. The browser keeps full trust (Herd CA is in the OS store).
- [x] **`setup-unverified-user` test-helper route added** (`routes/test-helpers.php`). Needed because the Verify-email a11y test registered via live UI, and registration triggers the `VerifyEmail` notification synchronously — that send only succeeds with the webServer's `MAIL_MAILER=array` override, which does **not** apply when base URL = Herd (real resend mailer rejects `@example.com`, so the `/verify-email` redirect never happened). The refactored `auth-a11y.spec.ts` test now creates an unverified logged-in user via the helper and navigates straight to `/verify-email`, staying green under either base URL while still exercising the real prompt page and signed-URL completion.
- [x] **Machine-local Edge fallback** (`tests/a11y/playwright.edge.config.ts`) added behind `.gitignore`, with `test:a11y:edge` npm script — sibling to the existing gitignored Chrome config. Both machine-local configs use the same base-URL helper.
- [x] **Verification.** Full a11y suite **19/19** against the Herd HTTPS base URL (system Chrome fallback, since bundled Chromium remains missing on this machine — CDN blocked, documented in ISSUE_REGISTER). `resolveBaseUrl()` verified three ways: env override wins, Herd probe defaults to `https://tamasharoom.test`, and the dev server is started normally by `webServer` regardless.
- [x] **Docs.** This entry added to `docs/TASK.md`.

### CI a11y failure triage + suite hardening (2026-08-10)

**Batch scope:** triaged 4 reported CI a11y failures against current code; fixed the 2 that genuinely reproduce (CI server env mismatch for the "Verify email" flow; load-dependent room-page navigation), made the contrast suite deterministic against mid-transition sampling, and documented the 2 stale/never-reproducing reports.

- [x] **Reported #1 (dark room-tab contrast 2.49:1, fg `#7f7f90` / bg `#43434b`): STALE — never existed.** `git log -p` over `resources/css/app.css` shows those hex values in no committed state; the palette-rollback/contrast batches (commits `3de87b3`, `12c5ea8`, `a918221`) already shipped the current dark tokens (`--muted-foreground: 240,10%,62%`, `--secondary: 240,12%,14%`). A browser probe of the settled, populated room page reports **0 color-contrast violations in both themes**; inactive tab = `rgb(148,148,168)` on `rgb(31,31,40)` ≈ **5.5:1** (dark) and `rgb(99,99,121)` on `rgb(232,232,238)` ≈ **4.79:1** (light). No code change needed.
- [x] **Reported #2 (auth-a11y "Verify email" `waitForURL` timeout): real CI bug fixed.** Root cause: `.github/workflows/ci.yml` pre-starts `php artisan serve` (step "Start Laravel Server") with only `SESSION_SECURE_COOKIE: false`, and both a11y/E2E Playwright configs use `reuseExistingServer: true` — so Playwright *reuses* that already-running server and the config's `MAIL_MAILER: array` / `PHP_CLI_SERVER_WORKERS: 4` webServer env never applies in CI. The reused server therefore serves the `.env` copy (`MAIL_MAILER=resend` from `.env.example`), `POST /register` fires the synchronous `VerifyEmail` notification at Resend with an `@example.com` address → `TransportException` → **500** → redirect never happens → timeout. **Fix:** the workflow step now sets `MAIL_MAILER: array` and `PHP_CLI_SERVER_WORKERS: 4`, so the reused server behaves exactly like the webServer-managed one. Verified: with that env the flow POSTs → 302 → `/verify-email` (no Resend call). Additionally bumped the two `waitForURL` timeouts `10000` → `30000` in `auth-a11y.spec.ts` (the 10s window is also tight on a busy single worker — the test passed in isolation at 2.7s but timed out at 11.4s under full-suite load).
- [x] **Reported #3/#4 (room "زیرنویس" / "اعضا" buttons never found, 30s timeout): navigation barrier hardened.** The room tests' `gotoRoom()` ended with `page.waitForLoadState("networkidle")` after loading the room page — but a live room page (video player, playback-sync + presence polls, heartbeat) never reaches networkidle, so under load the wait can stall/fail before the tab-bar buttons render. Replaced the networkidle barrier after room navigation with a deterministic wait on the always-first-paint `چت` tab button (`waitForRoom`) in both `contrast-a11y.spec.ts` and `room-a11y.spec.ts`. Verified: the subtitle-manager ("Room open states") and members-dialog ("Room dialogs") tests pass.
- [x] **Bonus: contrast suite made deterministic against mid-transition sampling.** While verifying #3/#4, 3 contrast tests failed intermittently with *blend* ratios (e.g. fg `#77778c` = midpoint of dark `rgb(148,148,168)` / light `rgb(99,99,121)` muted-foreground; `#9c9c9f`, `#a5a5aa` are not tokens at all). Probe confirmed the mechanism: with `channel: "chrome"`, Playwright does **not** emulate `prefers-reduced-motion` (`matchMedia(...).matches === false`), so the app's `transition-duration: 0.01ms` collapse never applies and the theme-toggle transition (150ms) is still sampling when axe runs. `setTheme()` now injects a stylesheet (`*{transition:none !important;animation:none !important}`) before each scan, so axe always reads settled tokens regardless of browser channel. Verified stable across 2 consecutive full-suite runs.
- [x] **Verification.** Full a11y suite **19/19**, run against system Chrome via the local `playwright.chrome.config.ts` (untracked convenience config, committed configs unchanged). `tsc`/lint/format scopes cover `resources/js` only — no app code changed in this batch.
- [x] **Docs.** This entry added to `docs/TASK.md`. Nothing committed/pushed.

### Error-text + primary-on-tint contrast batch (2026-08-10, committed as one logical commit)

**Batch scope:** `--destructive-text` token split; `text-primary` → `text-primary dark:text-accent-foreground` on tinted surfaces; semantic-token + muted-foreground lightening; three same-category misses caught by self-review and fixed before commit.

- [x] **`--destructive-text` token added** (`resources/css/app.css`): error *text* gets its own per-mode red instead of reusing the `destructive` fill. Light `347,77%,45%` (5.44:1 on card), dark `347,77%,60%`. Error text converted across `InputError`, `ui/input.tsx`, `room-chat` reconnect, `Dashboard`, `Rooms/Join`, `Rooms/Show` subtitle error, `SyncedVideoJsPlayer`. Dark value **bumped to 62%** (`#e95374`, hue unchanged) so the `SyncedVideoJsPlayer` error text — which sits on `bg-muted`, not `bg-card` — clears 4.5:1 (4.38→4.63 on muted; card stays a reasonable 5.33, not blown out).
- [x] **`text-primary` on tinted surfaces → `text-primary dark:text-accent-foreground`** (dark-mode primary is only 3.05:1 on card; accent-foreground is 11.77:1): 6 auth welcome lines, `Welcome` step circles, `room-card` badges + copy hover. `Rooms/Show` subtitle active rows `bg-primary/20 text-primary` → `text-accent-foreground`.
- [x] **Semantic-token + light-mode tightening:** `success` 42→31, `warning` 50→32, `muted-foreground` 45→43 (all verified 4.7–4.9:1 on card light); `green-500`/`red-500`/`yellow-500` icons → `text-success`/`text-destructive`/`text-warning`; subtitle-overlay pill contrast bumped.
- [x] **Three same-category misses found in review, fixed before commit:** (1) `ResponsiveNavLink.tsx` active mobile-nav item `bg-primary/10 text-primary` → `dark:text-accent-foreground` (+ focus variant); (2) `subtitle-settings.tsx` reset link `text-primary` → `dark:text-accent-foreground`; (3) `--destructive-text` dark 60→62 for the `bg-muted` player-error surface. Ratio-verified all three (10.99:1, 11.77:1, 4.63:1).
- [x] **Regression coverage added** (`tests/a11y/contrast-a11y.spec.ts`): new "Responsive nav (mobile viewport, menu open)" test (390×844, opens the hamburger, axe `color-contrast` both themes — the mobile nav was previously unscanned); the existing "Room dialogs" test now also sets a non-zero subtitle offset (scoped `dialog[open] input[type="range"][max="5000"]`, not `getByRole("slider")` — Video.js exposes its own sliders on the room page) to reveal and audit the reset link.
- [x] **Verification.** a11y **19/19** (was 18); Vitest **231/231**; `tsc` clean; ESLint clean; Prettier clean. E2E **21/22** on the system-Chrome workaround — the 1 failure is the **pre-existing** documented `tap-to-play` dev-server flake (this run resolved the overlay locator to Video.js's Chromecast cast button, `aria-disabled=true`; environmental, unrelated to contrast; see the "Pre-existing" note below).
- [x] **Environment note (same as prior batch).** Bundled Playwright Chromium binaries are missing on this machine (CDN region-blocked), so a11y/E2E ran against system Chrome via a temporary `playwright.chrome.config.ts` (deleted afterward; committed configs unchanged). `spawn php` in E2E `globalSetup` required prepending `C:\Users\Khashayar\.config\herd\bin\php84` to `PATH`.
- [x] **Docs.** This entry added to `docs/TASK.md`. Committed as one logical commit (18 batch files + the 3 fixes + the a11y spec).

### Dialog-primitive contrast fix: black text on dark cards (2026-08-10)

- [x] **Bug.** `resources/js/Components/ui/dialog.tsx` styled the `<dialog>` with `bg-card` but no text color, so all inherited-color text (dialog titles, plain labels, icon-only buttons, ghost buttons, the room-settings status line) rendered the UA default `CanvasText` (pure black) — **1.12:1** on the dark card. Consumers: `RoomSettingsDialog`, `SubtitleSettingsDialog`, and `ConfirmDialog` (used for subtitle-track delete, kick, transfer, dashboard/room-chat confirms). The legacy profile delete-account `Modal` (`DeleteUserForm.tsx`) sets its own `text-foreground` and was unaffected — which is why the existing contrast suite passed.
- [x] **Root cause of the coverage gap.** The a11y suite scanned the room page (chat + members tabs) and the subtitle *manager*, but never opened any dialog built on the new `Dialog` primitive — so black-on-dark dialogs passed every axe scan. The user's hypothesis (dialog-only component missed by the suite) was correct.
- [x] **Fix.** One line: `text-foreground` added to the `Dialog` element's base classes (`resources/js/Components/ui/dialog.tsx`), so the primitive sets its own foreground and all three consumers inherit a theme-correct color.
- [x] **Regression test.** New `tests/a11y/contrast-a11y.spec.ts` test "Room dialogs (settings + subtitle settings + confirm)" opens all three dialog types in **both themes** and runs axe `color-contrast` on each. **Revert-proof:** with the fix removed, the test fails (dark-mode room-settings dialog input reported at 1.2:1); with it, passes.
- [x] **Verification.** Full a11y suite **18/18** (was 17); Vitest **231/231**; `tsc` clean; ESLint clean; Prettier clean. E2E 21/22 on system-Chrome workaround — 1 failure is the **pre-existing** documented `tap-to-play` dev-server flake (TASK.md "Pre-existing" note; video stalls at currentTime 0, unrelated to dialogs; presence-moments flake passed on retry).
- [x] **Environment note.** Playwright's bundled Chromium binaries were missing from this machine and the CDN download failed, so the a11y/E2E suites were run against system Chrome 151 via a temporary `playwright.chrome.config.ts` (deleted afterward; committed configs unchanged). `php` resolves only via the Herd `.bat` shim on this box, so a `spawn php` failure in E2E `globalSetup` required prepending `php84` to `PATH`.

### Windows dev-server env-var known issue documented (2026-08-10)
- [x] Diagnosed + documented a Windows-only quirk in the `debugging` skill (KI-016): Laravel's `php artisan serve` (13.20.0) passes only `$passthroughVariables`-whitelisted env vars to the child server process on Windows, so Playwright's `webServer.env: { MAIL_MAILER: "array" }` never reaches the test server — it falls back to `.env`'s `MAIL_MAILER=resend`, the synchronous `VerifyEmail` notification hits Resend, rejects `@example.com`, and `POST /register` 500s (the "Verify email" a11y test times out). On Linux/CI the canonical config works (TAM-010). Verified workarounds: (a) temporarily set `MAIL_MAILER=array` in `.env` and restore after (full a11y **17/17**); (b) `php artisan serve --no-reload` passes shell env through (`POST /register` → 302 → `/verify-email`, no Resend call); (c) a direct `php -S` server inherits the parent's env vars. Committed as a docs-only commit.

### UI primitives batch: shadcn-style `ui/` layer, Sonner toasts, Frimousse emoji picker + dark indigo palette (2026-08-08)

**Batch scope:** STEP 1 (E2E flake), STEP 2 (rtl-i18n-policy skill), STEP 3a/3b/3c (UI primitives + palette), STEP 4 (docs drift).

**STEP 1 — E2E 2.3 flake fixed** (see the dedicated entry below; recorded there).

**STEP 2 — rtl-i18n-policy skill.** `.opencode/skills/rtl-i18n-policy/SKILL.md` created exactly as specified; not yet entered in the AGENTS.md skill table (outside this batch's listed scope).

**STEP 3a — Frimousse emoji picker in chat.**
- New `resources/js/Components/ui/emoji-picker.tsx` (headless Frimousse render-prop, LTR/neutral per rtl-i18n-policy) and `resources/js/Components/ui/popover.tsx` (Radix anchored popover, theme tokens).
- `room-chat.tsx`: new `Smile` trigger button (aria-label "افزودن شکلک") + popover inserting the selected emoji at the cursor position and closing; composer otherwise unchanged.
- Verified: type-check, lint, Prettier clean; Vitest 231/231; chat E2E 3/3.

**STEP 3b — Toast system replaced with Sonner.**
- Deleted `resources/js/Hooks/use-toast.ts` and `resources/js/Components/composite/toast.tsx`; new `resources/js/Components/ui/sonner.tsx` (theme-token styled, RTL container) mounted **globally** in `app.tsx` inside `ErrorBoundary` — also fixes the old per-Page mount gap on Profile/Auth/Welcome.
- All 6 call sites (member-list, room-settings, Show, Dashboard, etc.) now `import { toast } from "sonner"`; both unit-test mocks updated.
- Verified: type-check, lint, Prettier clean; Vitest 231/231.

**STEP 3c — shadcn-style primitives + dark indigo/rose palette + Inter.**
- Radix primitives + CVA installed (`@radix-ui/react-*` ×10, `class-variance-authority`, `tailwind-merge` already present); `ui/` layer expanded with shadcn conventions.
- **Magic UI explicitly deferred for MVP** (decorative/motion-first components fail the scope firewall) — recorded in DECISION-017.
- Palette reworked in `resources/css/app.css` (`@theme` + `:root` + `.dark`): **#0A0A0F** near-black bg (dark), indigo primary #6366F1 at full strength only in `--ring`/`--info`/tints; solid fills use AA-safe 600-level #4F46E5 / #E11D48 (white text ≥4.5:1 both modes). Cool indigo-tinted grays; `warning` keeps amber. `--radius`, z-scale unchanged.
- Fonts: Vazirmatn stays the Persian primary; **Inter Variable (`@fontsource-variable/inter`)** added as the Latin-glyph fallback (`fonts.css` imported before; stack `'Vazirmatn','Inter Variable',…` in `@theme` + `html`). Preload unchanged.
- Propagated brand color off amber: `app.blade.php` theme-color → `#0A0A0F`; error pages 404/500 → `#0a0a0f`/`#6366f1`/`#f43f5e`; `ApplicationLogo` rect fill → `var(--color-primary,…)` fallback `#4F46E5`, text → `var(--color-primary-foreground)`; Inertia `progress.color` → `#6366F1`.
- **Real contrast fix on the landing page:** invite-code chip `bg-primary-foreground/20` → `bg-black/20` (white text needs ≥4.5:1 on indigo; the previous tint produced 4.11:1).
- Verified: build OK (Inter woff2 bundles included), type-check, lint, Prettier clean; Vitest **231/231**; backend `php artisan test` **262/262** (1942 assertions); E2E **22/22** (4.7m); a11y **10/11** — the 1 failure was the **pre-existing** `auth-a11y` "Verify email" registration test (waitForURL timeout on the registration redirect, unrelated to this batch; fails on a clean checkout too). Welcome a11y (previously broken by the palette at 4.11:1) now passes. **TAM-010 is now RESOLVED — see the dedicated entry below and `docs/ai/ISSUE_REGISTER.md`.**

**STEP 4 — docs drift reconciled.**
- `design-systems/tamasharoom/DESIGN.md` rewritten: typography (Inter = Latin loader companion, not "everything" anti-pattern); color system → dark indigo/rose with the AA fill rationale (600-level fills, brand hues reserved for ring/info), indigo-tinted gray scale, Persuade/Operate color notes, Implementation Constraints now document the two-tier primitive policy (shadcn/ui Radix layer for new primitives; Headless UI/native dialog for legacy modals).
- `docs/PROJECT.md`: UI Components row now documents Headless UI + shadcn-style `ui/` layer; fonts.css line updated.
- `docs/quality-report.md`: palette/architecture summary lines corrected.
- `docs/ai/DECISION_LOG.md`: DECISION-017 (shadcn-style primitives + Sonner + Frimousse; Magic UI deferred) and DECISION-018 (dark indigo/rose palette + Vazirmatn/Inter).
- `npm run check:docs` clean (47 files). Nothing committed/pushed.

### TAM-010 RESOLVED: auth-a11y "Verify email" flow + Inertia progress-bar ARIA (2026-08-08)

The pre-existing `auth-a11y` "Verify email page" failure (tracked as **TAM-010**) is **resolved**, and the fix surfaced + fixed a real product a11y defect. Full record in `docs/ai/ISSUE_REGISTER.md` (moved to RESOLVED).

- [x] **Root cause of the registration redirect timeout.** Playwright's `webServer` boots `php artisan serve` against the local `.env` (`MAIL_MAILER=resend`, real key). Registration fires `Registered` → `sendEmailVerificationNotification()` → the `VerifyEmail` notification (framework base, not `ShouldQueue`) sends **synchronously** → Resend rejects `@example.com` test addresses (`Invalid "to" field…`, `ResendTransport.php:118`) → `TransportException` → **500 on POST /register** → redirect never happens → 10s `waitForURL` timeout. `phpunit.xml` already sets `MAIL_MAILER=array` (PHPUnit never touched Resend); there is no `.env.testing`; only the Playwright boot was the culprit. **Hypothesis check:** the user's guess (test env still on `resend`) was wrong at the location level but right at the mechanism level.
- [x] **Fix (test-server scoped; local/production `.env` untouched).** `MAIL_MAILER: "array"` added to `webServer.env` in `tests/a11y/playwright.config.ts` and `tests/e2e/playwright.config.ts`. Because `Notification::fake()`/`Mail::fake()` are PHPUnit-only, the browser test reads the real signed URL from a new test-only route `GET /__test/verification-url?email=…` (`routes/test-helpers.php`, returns `URL::temporarySignedRoute('verification.verify', now()->addMinutes(60), ['id' => $user->id, 'hash' => sha1($user->getEmailForVerification())])`), then `page.goto()`s it and asserts landing on the verified-only `/dashboard`. The assertion targets `/dashboard` (not `?verified=1`) because the unverified-dashboard redirect stores `/dashboard` as the session `intended` URL, which `intended()` honors over the controller's fallback.
- [x] **Real product defect surfaced & fixed.** Once registration succeeded, axe flagged Inertia's bundled NProgress markup `<div class="bar" role="bar">` — `role="bar"` is an invalid ARIA role (critical). Fixed in `resources/js/app.tsx` (`progress: false`) with a new `resources/js/Components/ui/inertia-progress.tsx`: thin `role="progressbar"` (`aria-valuemin/max/now`, Persian `aria-label`) driven by `router.on('start'|'progress'|'finish')`, `bg-primary`. No custom component/template is exposed by `@inertiajs/react` 2.3.27's `progress` option (`false | { delay, color, includeCSS, showSpinner }`), so disabling the default + a custom bar is the only clean path.
- [x] **Verification.** `-g "Verify email" --repeat-each=5` **5/5**; full a11y **11/11**; full E2E **22/22** (first run had a transient `playback-sync 2.1` sync-error-banner flake — green in isolation and on the full re-run; the documented single-worker load flake, not a regression); `php artisan test` **262/262**; Vitest **231/231**; lint / type-check / Pint / Prettier clean; `npm run build` clean.
- [x] **Docs.** `docs/ai/ISSUE_REGISTER.md` TAM-010 moved to RESOLVED with root cause + fix record. Nothing committed/pushed (waiting on go-ahead).

### Email verification with Resend (2026-08-08)
- [x] **Resend transport.** Laravel's built-in `resend` `MAIL_MAILER` driver used; `resend/resend-php` SDK added (`composer.json` pins `1.0`). `.env.example` mail block updated to `MAIL_MAILER=resend`, `RESEND_KEY=` (blank placeholder — real key intentionally never written), `MAIL_FROM_ADDRESS="noreply@tamasharoom.ir"`, `MAIL_FROM_NAME="TamashaRoom"`. `config/services.php` `resend.key` reads `RESEND_KEY`.
- [x] **Gate real accounts only.** `app/Models/User.php` implements `Illuminate\Contracts\Auth\MustVerifyEmail` (was commented out); `hasVerifiedEmail()` overridden to `$this->isGuest() || parent::hasVerifiedEmail()` so guest accounts (`guest-{uuid}@tamasharoom.local` synthetic emails, never verified) stay exempt. The existing `['auth','verified']` middleware on the main app route group (`routes/web.php:30`) now actually enforces: unverified real users are redirected to the verification prompt until they verify.
- [x] **Persian verification email.** New `app/Notifications/VerifyEmail.php` extends the framework's signed-URL `Illuminate\Auth\Notifications\VerifyEmail`; Persian subject `خوش آمدید به تماشاروم — تایید ایمیل`, greeting, `تایید ایمیل` action button, ignore-instruction line, `تیم تماشاروم` salutation. Email HTML renders from the framework-bundled `notifications::email` view (no `vendor:publish` needed).
- [x] **Tests.** `tests/Feature/Auth/EmailVerificationTest.php` +6 (unverified real user redirected; verified user passes; guest exempt; guest join never prompts; registration sends Persian `VerifyEmail`; guest gets no verification email). Filter-run 9/9. Full `php artisan test` **262/262** (1942 assertions). Vitest 231/231. `npm run type-check`, `npm run lint`, `npm run format:check`, `npm run check:docs` all clean.
- [x] **E2E.** `npm run test:e2e` **22/22** (6.2m). One earlier full-suite run showed `lock-kick-transfer` + `keyboard-a11y` network-idle timeouts — both pass in isolation (5/5) and on the clean re-run; consistent with the documented single-worker load-flake pattern in this file, not a regression.
- [x] **Docs.** `docs/ai/DECISION_LOG.md` DECISION-016 (Resend transport, `MustVerifyEmail` with guest exemption). Nothing committed/pushed.

### E2E flake fix: playback-sync 2.3 out-of-order PATCH guard (2026-08-08)
- [x] **Root cause of the 2.3 flake (intermittent 50% failure under `--repeat-each`).** The test's first UI action was clicking the play/pause button. When `ensurePlaying` had already left the video playing, that click *pauses* it, and `handlePause`'s `syncImmediate` can be **swallowed by the `applyingRef` gesture guard** (`SyncedVideoJsPlayer.tsx:166-173`, guard window 100 ms) if a poll-driven apply lands in the same window. Result: no PATCH from the click, the video is paused, and the only PATCH to fire is the ArrowRight seek — which gets held. A paused video emits no `timeupdate`, so `patchCount` stays 1 and the test times out at `toBeGreaterThanOrEqual(2)`. Debug logging (added during diagnosis, removed after) confirmed: failing runs held the seek PATCH (`is_playing:true, position 5.99`) while the video was `paused:true`; passing runs held the toggle's pause PATCH and got the seek + timeupdate PATCHes afterwards.
- [x] **Fix (`tests/e2e/playback-sync-verification.spec.ts`).** The test no longer relies on a play/pause toggle producing a PATCH (guard-raced). It seeks first via the slider (`ArrowRight`) — `handleSeeked` **never** consults `applyingRef`, so PATCH #1 (held) is reliable — then waits for the first PATCH to be confirmed held (`patchCount >= 1` + `heldReq` non-null) and issues a **second, spaced seek after a 1.2s margin** so PATCH #2 is produced DETERMINISTICALLY. The seeked path always emits a PATCH, unlike the throttled+debounced `timeupdate` sync, which only fires while `timeupdate` events keep coming and can stall if the media pauses while buffering (a paused video emits no `timeupdate`, so `patchCount` could stay 1 and the test would time out at `toBeGreaterThanOrEqual(2)`). The spacing prevents the media element from coalescing the two quick seeks into a single `seeked`/PATCH. PATCH #2 forwards to the real server and bumps `state_version`; the stale synthesized response (`state_version: 0`) is then released and asserted ignored, as before. All temporary `console.log` debug lines removed.
- [x] **Verification** — test 2.3 `--repeat-each=5` **5/5**; full `playback-sync-verification.spec.ts` **4/4** (2.6m). `npm run lint`, `npm run type-check`, `npx prettier --check tests/e2e/playback-sync-verification.spec.ts` all clean. Nothing committed/pushed.

### Step 3 — Pusher Real-Time Sync: Polling Integration Point Inventory (2026-08-07)

**Purpose.** This section inventories every polling integration point BEFORE converting to Pusher push (Step 3). It records what triggers updates, the expected data shapes, and how the frontend hooks consume them, so the push rewrite (and the eventual Reverb migration) can be verified feature-for-feature.

**Transport-agnostic backbone (already in place, reused by this step).** Every room-wide state change is already written as a Laravel broadcastable Event (`ShouldBroadcast`) on `PresenceChannel("room.{id}")`:

- `App\Events\PlaybackStateChanged` — `broadcastAs: playback.state.changed`; payload `is_playing`, `position_seconds`, `duration_seconds`, `playback_rate`, `video_url`, `state_version`, `server_timestamp`, `user_id`, `updated_at`. Dispatched with `->toOthers()` by `PlaybackController::update` and `::setVideo` after the authoritative write.
- `App\Events\MemberPresenceChanged` — `broadcastAs: member.presence.changed`; payload `members` (array of the exact shape `PresenceController@index` returns). Dispatched (NOT `->toOthers()`) by `PresenceService::heartbeat` / `::leave`.
- `App\Events\NewChatMessage` — `broadcastAs: chat.message.new`; payload `id`, `user_id`, `body`, `user:{id,name}`, `created_at`. Dispatched with `->toOthers()` by `ChatController::store`. **Remains polling** (scope decision below).
- Today `BROADCAST_CONNECTION=log` in `.env` (`.env.example` has `null`), so `broadcast()` only logs locally; the frontend never listens to these events — it polls the JSON endpoints instead.

**Frontend hooks — what moves to push.**

1. `use-playback-sync.ts` (`resources/js/Hooks/use-playback-sync.ts`) — consumed by `SyncedVideoJsPlayer.tsx` (every room member, host and guest).
   - **Read (poll):** `GET /playback/{room}/state` on a tiered cadence — 3 s while playing, 10 s while idle; immediate refetch on tab-visible; AbortController-guarded. Returns `PlaybackStateResponse` (snake_case) → `toPlaybackState()` → `PlaybackState` (camelCase). A `state_version` guard drops stale snapshots (`incoming.stateVersion <= versionRef.current`). `receivedAt` (client-clock epoch) seeds `computeExpectedPosition()` for drift extrapolation; corrected state is surfaced via `onRemoteChange`.
   - **Write (unchanged by this step):** host-only `sync(partial)` → `PATCH /playback/{room}` (debounced 1 s) and `syncImmediate(partial)` → PATCH then refetch. `PlaybackSyncResponse.state_version` advances the local guard only when strictly newer (guards out-of-order PATCH responses).
   - **Push events to consume:** `playback.state.changed` (server already applies `->toOthers()`, so the sender's own channel message is excluded; a client-side `user_id` self-check is belt-and-braces). Same `PlaybackStateResponse` shape → `toPlaybackState()` → same `state_version` guard + drift path, so the hook's public surface (`state`, `loading`, `error`, `sync`, `syncImmediate`) is unchanged.

2. `use-presence.ts` (`resources/js/Hooks/use-presence.ts`) — consumed by `Pages/Rooms/Show.tsx` via `usePresence(room.id, { onRemoved })`.
   - **Read (poll):** `GET /presence/{room}` every 5 s. Returns `PresenceMember[]` (`id`, `user_id`, `name`, `presence_status`, `last_seen_at`, `disconnected_at`, `joined_at`, `is_owner`). `derivePresenceMoments()` diffs the incoming list against `baselineRef` → `PresenceMoment[]` appended to `moments`; `buildPresenceBaseline()` reseeds the baseline. A 403/404 (kicked/removed) → `onRemoved()` and the page redirects.
   - **Heartbeat (write, unchanged by this step):** `POST /presence/{room}/heartbeat` every 30 s (exponential backoff to 5 min); returns `heartbeat_version`. `connected` is owned by the heartbeat — the single source of truth. Leave beacon `POST /presence/{room}/leave` via `navigator.sendBeacon` on unload / SPA unmount.
   - **Push events to consume:** `member.presence.changed` (`members` array replaces the poll result; moments derivation and `onRemoved` logic stay). Note this event is NOT `->toOthers()`, so the dispatcher also receives it — the hook must tolerate self-authored presence snapshots.

3. `RoomChat` (`resources/js/Components/composite/room-chat.tsx`) — consumed by `Show.tsx`. **Stays on polling** (explicit scope decision, below): `GET /chat/{room}/messages` every 3 s, unread counting by last-seen message id, `chatMessagesSchema` (Zod) validation. Writes: `POST /chat/{room}/messages`, `DELETE /chat/{room}/messages/{message}`.

**Explicit scope decision.** Playback sync and presence move from polling to Pusher push. Chat polling stays as-is unless explicitly told otherwise. The `NewChatMessage` event already exists on the same channel, so converting chat later is a one-hook change.

**Authorization model to preserve.** The current polling endpoints enforce membership via policies (`PlaybackController::state` / `PresenceController::*` → `authorize('memberAccess', $room)`). The private/presence-channel auth callback in `routes/channels.php` already mirrors this (`memberAccess` gate, returns `['id','name']`), so channel auth for Pusher must keep that exact gate — no weakening.

### Step 3 — Pusher Push Transport: hook test coverage (2026-08-07)
- [x] **Push-path unit tests for both hooks** — `use-playback-sync.test.tsx` (+5) and `use-presence.test.tsx` (+6) now run their real push branches via the in-memory `__tests__/helpers/fake-echo.ts` (`getEcho()` mocked through a `vi.hoisted` holder; polling default unchanged). Playback covers: joins `presence-room.{id}`, applies a broadcast snapshot through the `state_version` guard (stale drops), re-seeds from authoritative GET on socket reconnect, no tiered polling loop while push is active, and channel leave on unmount. Presence covers: `here`/`joining`/`leaving` roster snapshots, a `.member.presence.changed` payload roster, reconnect re-seed, no polling interval in push mode, and channel leave on unmount.
- [x] **Fixed a pre-existing hook lint error in `use-presence.ts`** — `usePage()` was called inside a `useEffect` callback (rules-of-hooks error + react-compiler warning). It is now read at the hook top level inside the try/catch (still Inertia-agnostic for component tests) with the result stored to `currentUserIdRef` via an effect (react-hooks/refs). Also dropped an unused `channel` key from `fake-echo.ts` that failed `tsc`.
- [x] **Verification** — Vitest **43/43** (the two hook suites); `tsc --noEmit` clean; ESLint 0 errors / 0 warnings; Prettier clean. Nothing committed/pushed.

### Pusher Push Transport: broadcast delivery for E2E (2026-08-07)

**Problem.** The `presence-moments` E2E (join + leave system messages) was failing on the leave moment, and the wider push-mode suite was broken, because server→Pusher broadcasts were never delivered. Two independent root causes, both diagnosed with measurements:

- **Broadcasts were queued to the database and never processed.** `QUEUE_CONNECTION=database` with no worker means every `broadcast(new MemberPresenceChanged(...))` / `PlaybackStateChanged(...)` lands as an `Illuminate\Broadcasting\BroadcastEvent` job that sits in `jobs` forever (5,499 accumulated locally). The join moment worked only because it rides Pusher's native presence `joining` event (browser↔Pusher, no server API); the leave moment requires the `.member.presence.changed` server broadcast, which never arrived. This matches the documented production design (`SYSTEM.md` §18.07): broadcasts are queued and drained by the scheduled cron `queue:work --stop-when-empty --max-time=30` — but locally/E2E no scheduler runs.
- **PHP could not do HTTPS to Pusher at all.** `curl.cainfo`/`openssl.cafile` were empty on this Windows dev box, so every Pusher API call threw `cURL error 60: SSL certificate ... unable to get local issuer certificate` (verified via a direct `Pusher::trigger` from a script).

**False start (not shipped).** Setting `QUEUE_CONNECTION=sync` so broadcasts dispatch in-request was measured to stall the server: each Pusher API round-trip takes ~1.3 s from this machine, and playback sync broadcasts ~1/sec while playing, saturating all four `PHP_CLI_SERVER_WORKERS` (guest page document load ballooned to 25 s, React never mounted, `#app` empty). Synchronous HTTPS in the request path is untenable on the single-core budget.

**Fix (production design, applied).**
- `.env` keeps `QUEUE_CONNECTION=database` and `BROADCAST_CONNECTION=pusher`; no `.env.example` change.
- Local machine: downloaded the official CA bundle to `C:\php84\extras\ssl\cacert.pem` and enabled `curl.cainfo`/`openssl.cafile` in `C:\php84\php.ini` (required for any local Pusher testing).
- Cleared the 5,159 stale queued broadcast jobs and `failed_jobs` (`php artisan queue:clear` / `queue:flush`) left over from the broken config.
- `tests/e2e/globalSetup.mjs` (new) + `playwright.config.ts`: E2E now starts a draining `php artisan queue:work --tries=3` via `globalSetup` and kills it on teardown. Chosen over a webServer-command wrapper because the webServer uses `reuseExistingServer: true` (reuses a developer's own `php artisan serve`), so a webServer command never runs when a server already exists — the worker is needed regardless. Harmless in CI (`BROADCAST_CONNECTION=null` drains no-op broadcasts).

**Verification.** Full `npm run test:e2e` **22/22** (4.5 m) with the auto-started worker; worker logs show `MemberPresenceChanged` / `PlaybackStateChanged` / `SubtitleDefaultChanged` jobs delivered in ~260 ms each. `tsc --noEmit`, ESLint, Prettier clean. Pre-existing playback-sync flakes (2.2: 95 s `sync-sample.webm` fixture reaches its end mid-assertion; 2.3: seek-to-PATCH timing race) pass in isolation and are unrelated to the delivery change. Nothing committed/pushed.

**CSP test fix (2026-08-07).** `SecurityTest::csp_connect_src_allows_the_pusher_websocket_when_broadcasting_is_pusher` failed in CI with `wss://ws-.pusher.com` (empty cluster). Root cause: the test's `putenv('PUSHER_APP_CLUSTER=ap1')` is ineffective — Laravel's `env()` reads `$_ENV` first, which the bootstrapped `.env` (copied from `.env.example` with an empty `PUSHER_APP_CLUSTER=`) already populated. Fix: `SecurityHeadersMiddleware::pusherWebSocketOrigin` now reads the cluster from `config('broadcasting.connections.pusher.options.cluster')` with an `mt1` fallback for empty values (so an empty env can never emit `ws-.pusher.com`); the test pins the cluster via `config([... => 'ap1'])` instead of `putenv`; and `.env.example` now ships `PUSHER_APP_CLUSTER=ap1` / `VITE_PUSHER_APP_CLUSTER=ap1`. Verified: `php artisan test --filter=SecurityTest` 21/21 (also passes with an empty cluster in `.env`, simulating CI); full backend 256/256; Pint clean.

### Guest Join Mode (2026-08-07)
- [x] **Guests can join a watch room without an account** - invite-link recipients reach `/rooms/join/{inviteCode}` GET/POST **outside** the `['auth','verified']` group (still `throttle:join`); the controller creates a real `users` row with `is_guest=true` under a randomized unique `guest-{uuid}@tamasharoom.local` email + random password. Guests are still real users, so `RoomPolicy`, chat, playback, presence, and subtitles all work unchanged. Because `User` does not implement `MustVerifyEmail`, the `verified` middleware passes for guests.
- [x] **`users.is_guest` migration** - `2026_08_07_000001_add_is_guest_to_users_table.php` (boolean, default false, after `password`). `User` gains `is_guest` fillable/cast + `isGuest()`.
- [x] **Guest branch in `RoomController@join`** - creates the guest identity and `Auth::login`s it only *after* `Gate::forUser($user)->authorize('join', $room)` succeeds (full/locked rooms never leave an orphan guest row). Authenticated joiners are never marked `is_guest` and follow the existing path.
- [x] **Name-only join UI** - `Rooms/Join.tsx` branches on `auth.user`: guests see a display-name-only form (no email/password/login); authenticated users see the existing confirm prompt. Layout branches between `GuestLayout` (no header) and `AppLayout`.
- [x] **Blank name fallback** - missing/blank `guest_name` falls back to `مهمان` (max 60 chars, validated in `JoinRoomRequest`).
- [x] **Tests** - `tests/Feature/GuestJoinTest.php` (8 tests: guest confirm view, join with name, blank-name fallback + auth asserted, full/locked room rejection leaves **no** guest row, authenticated joiner stays non-guest, synthetic unique email). All `RoomManagementTest` auth-join flows still green.

### Player Redesign - Custom Controls (2026-08-07)
- [x] **Rebuilt the control overlay on native `<video>`** (no library). The seekbar now renders three stacked layers: a buffered rail (`video.buffered`) behind a brightness-primary played fill behind a rounded thumb, over a taller hit area. Time is a `tabular-nums` current/duration readout; volume slider, mute, and fullscreen kept. `bg-gradient-to-t` overlay opacity still only hides for pointer-fine hover/focus.
- [x] **Keyboard shortcuts scoped to the player** - `Space` play/pause (host only), `ArrowLeft/Right` seek 10s (host only), `m` mute, `f` fullscreen. Reader listens on the focusable wrapper (`tabIndex=0` + `aria-keyshortcuts`) and ignores INPUT/TEXTAREA/SELECT/BUTTON/role=slider so the chat box and the seek slider are never hijacked.
- [x] **RTL + responsive** - progress tracks are measured, not directional, so they stay invariant under RTL; controls wrap cleanly on mobile (`h-5/h-6` icon breakpoints, volume slider narrows via `sm:w-24`).
- [x] **Subtitle overlay verified** - `SubtitleOverlay` remains a sibling of the `<video>` read via the shared `videoRef`; its `bottom-16` sits above the redesigned control bar; offset (`currentTime*1000 + settings.offset`) and bottom/top positioning unchanged and covered by existing tests.
- [x] **Sync contract untouched** - no change to `use-playback-sync`, `sync`, `syncImmediate`, drift/ended/tap-to-play/error/empty logic. Only presentation + shortcuts changed.
- [x] **Tests** - 6 new FE tests (`video-player.test.tsx`): space toggles host play/pause from the wrapper, space ignored on focused input, `m` mute, wrapper ArrowRight seeks 10s, slider-focus arrows don't double-seek, buffered+played rails render. Existing 30 pinned tests still green (36 total).

### Step 2: Video.js v10 Player Migration (2026-08-07) — supersedes "Step 1: Plyr Player Migration" below (see DECISION-014)
- [x] **Rebuilt the player on Video.js v10 (`@videojs/react` 10.0.0-beta.26)** — `SyncedVideoJsPlayer` → `VideoJsPlayer` replace `SyncedPlyrPlayer`/`PlyrPlayer` (deleted). `plyr-react` removed from `package.json`; `@videojs/react` (already present) now drives the UI. `Player.Provider` → `I18nProvider locale="fa"` (+ `@videojs/react/i18n/locales/fa/register`) → `VideoSkin` + `<Video ref>`; the built skin CSS (`@videojs/react/video/skin.css`) replaces the plyr stylesheet in `resources/css/app.css`.
- [x] **Source changes via the documented `store.loadSource()` API** — v10 mutates the SAME `<video>` element (`media.src = src; media.load()`), so the native `play`/`pause`/`seeked`/`timeupdate`/`ended`/`error` listeners the sync shell binds are bound **once** and survive every source swap. No `react-aptor` destroy/recreate, no ~200 ms watchdog re-bind (the Plyr-specific fixes). `loadSource` throws before the Provider's effect attaches the store, so the load effect gates on `store.target` with a `setTimeout(0)` retry.
- [x] **Proxy→direct transport fallback preserves position** — `lib/player-source.ts` (`shouldPreservePositionOnSourceChange`) decides when a source change is the same content on a new transport (preserve `currentTime`, clamped to the new duration) vs. a genuinely new video (reset — `PlaybackController@setVideo` zeroes `position_seconds` anyway). The decision runs inside the player's load effect (refs, not render), keeping the shell and lint clean.
- [x] **Sync contract preserved** — `use-playback-sync`/`sync`/`syncImmediate` untouched; host authority, guest drift (`computeExpectedPosition`), `applyingRef` guard, autoplay-block overlay, end card, and subtitle overlay all moved into `Components/Player/SyncedVideoJsPlayer.tsx`. Imperative handle contract (getCurrentTime/getDuration/getVolume/isPlaying/seekTo/play/pause/toggleMuted/toggleCaptions/enterFullscreen/getVideoElement) kept so the shell and `SubtitleOverlay` need no changes.
- [x] **Persian (RTL) UI** — v10's built-in fa locale: play "پخش", pause "توقف", seek slider "جستجو", tooltips; volume/time sliders are `role="slider"` with translated labels.
- [x] **Player layout is LTR, labels stay Persian** — the skin inherits `dir="rtl"` from the document root, so its logical CSS mirrored the whole UI (seek bar ran right-to-left, time/controls reversed). The player wrapper in `SyncedVideoJsPlayer.tsx` now pins `dir="ltr"` (prop-drills to the skin root via DOM inheritance, including fullscreen), giving the standard player layout while the `fa` locale still supplies Persian tooltips/aria-labels — labels are text, not layout. Type-check/lint/Prettier clean; `videojs-player.test.tsx` **21/21**.
- [x] **E2E selectors updated** — `playback-sync-verification.spec.ts` now targets `button.media-button--play` (JS-clicked because the controls are `pointer-events:none` while idle) and the seek slider by name "جستجو". All overlay/`شروع پخش` selectors were already player-agnostic.
- [x] **Tests** — new `__tests__/videojs-player.test.tsx` (22 tests, mock `VideoJsPlayer` + `usePlaybackSync`) + `__tests__/player-source.test.ts` (5 tests); `plyr-player.test.tsx` deleted. Vitest **220/220**; E2E playback-sync-drift **4/4**, tap-to-play **2/2**, room/chat/subtitle/presence/lock-kick/keyboard-a11y **16/16**; a11y **11/11**; `tsc`/ESLint/Prettier/`npm run build` clean. Show-page chunk grows (~97 kB gzip) with the v10 skin.

### Step 1: Plyr Player Migration (2026-08-07) — replaces "Custom Controls" above — SUPERSEDED by "Step 2: Video.js v10 Player Migration" above (2026-08-07, DECISION-014)
- [x] **Adopted `plyr-react` + `plyr`** for the player UI (see DECISION-013; supersedes DECISION-012). `show`/`room` media pages render `SyncedPlyrPlayer` → `PlyrPlayer`. The hand-rolled control overlay, its keyboard handler, and the buffered seekbar are gone (`composite/video-player.tsx` + `__tests__/video-player.test.tsx` deleted).
- [x] **Sync contract preserved** - `use-playback-sync` / `sync` / `syncImmediate` untouched; events, poll hook, and the transport-agnostic polling-to-WebSocket swap are unchanged. Host authority, guest drift correction (`computeExpectedPosition`), autoplay-blocked overlay, proxy→direct fallback, end card, and subtitle overlay all moved into `Components/Player/SyncedPlyrPlayer.tsx`.
- [x] **Plyr quirks solved** (the parts that break naive integration): bind listeners to the **media element** (`player.media`), not the instance (`timeupdate` never forwards); **watchdog re-bind** every ~200 ms when react-aptor recreates the player on `source` change; never read placeholder fields off `apiRef.current.plyr` before the real instance mounts (it's a Proxy where any property read returns a no-op function); guard programmatic `play()`/`pause()` in the apply effect with an `applyingRef` so the native events they fire don't re-enter sync and yank the host back; host play/pause/seek syncs include `positionSeconds` so the server never sees a stale `0`.
- [x] **Persian (RTL) UI** — `i18n` map (`پخش`/`مکث`/`موقعیت پخش`/...), Plyr `.css` imported, brand var `--plyr-color-main: #ef4444`, native `<track kind="captions">` from `subtitleSrc`. E2E selectors moved to the stable `.plyr__controls button[data-plyr="play"]`.
- [x] **Tests** - new `__tests__/plyr-player.test.tsx` (20 tests, mock `PlyrPlayer` + `usePlaybackSync`): event wiring, drift guard, autoplay-block overlay, replay, single play, guest no-control, host sync payloads. Vitest suite **214/214**; E2E playback-sync-drift suite **4/4** and tap-to-play **2/2** (the two suites that cover the room playback surface); a11y **11/11**; `tsc`/ESLint/Prettier/`npm run build` clean.

### Register Latency Profile (2026-08-07)
- [x] **Profiled `POST /register` end-to-end** with a non-invasive CLI harness (temporary script, no app code changed) against local drivers (database session/cache/queue, `MAIL_MAILER=log`, **bcrypt cost 12**). Averages over 3 runs: validation ~10.8 ms, isolated password hashing ~207 ms, `User::create` (incl. `hashed` cast) ~217 ms, `event(Registered)`+listener ~0.2 ms, `Auth::login`+session ~1.5 ms, DB queries ~5 ms/3 per run. Controller body ~235 ms.
- [x] **Confirmed no ~30 s blocking path** in `POST /register` — no mail (`User` has no `MustVerifyEmail`; `SendEmailVerificationNotification` early-returns), no queued listeners/jobs, no external/pwned password validation (defaults not customized), no sleeps or socket timeouts. The ~30 s figure in project docs is the cron queue drain (`--max-time=30`), not this route.
- [x] **bcrypt cost 12 is the only potential bottleneck** (~200+ ms; likely >300 ms on the single-core cPanel host). Decision: **keep cost 12 until post-deployment cPanel benchmarking**; only lower `BCRYPT_ROUNDS` to 11 in production `.env` if real registration latency confirms it (a config-only change, never code).
- [x] **No code changes.** Full evidence + decision in `docs/ai/PERFORMANCE_REGISTER.md`.

### MySQL Production Schema Migration (2026-08-07)
- [x] **Audited schema** — every table used by `app/Models` and every Laravel database-backed driver default (config/session → `sessions`; config/cache → `cache`/`cache_locks`; config/queue → `jobs`/`job_batches`/`failed_jobs`; auth → `users`/`password_reset_tokens`) already has a migration. No independent `playback_states` table exists (state lives on the `rooms` row). **No missing migrations found, none added, no existing migration modified.**
- [x] **`database/tamasharoom.sql`** — MySQL-only DDL derived solely from the migrations (14 tables: 13 app/framework + `migrations` repo), InnoDB, `utf8mb4_unicode_ci`. **Fix vs first attempt:** first draft (`schema_mysql.sql`) failed phpMyAdmin import with `#1005 / errno 150` because it created `subtitle_tracks` with an inline FK to `rooms` before `rooms` existed. Regenerated into `tamasharoom.sql` as **two phases**: all `CREATE TABLE`s carry no foreign keys, then after every table exists all 8 FKs are attached via `ALTER TABLE ... ADD CONSTRAINT`. Zeros out forward references (incl. the circular `rooms` ↔ `subtitle_tracks`), preserving every index/unique/cascade/nullability from the migrations. Old `schema_mysql.sql` removed.
- [x] **Wrote `docs/mysql-deployment.md`** — source-of-truth = migrations only; table dependency order; why the previous SQL failed; FK/index/unique/cascade/nullable verification table; cPanel/phpMyAdmin import steps; production `.env` DB values; post-import artisan commands; clean-import mental simulation with zero forward references.
- [x] **No behavior change** — no migrations/config/app changes; local `DB_CONNECTION=sqlite` default untouched; `mysql` connection block already present in `config/database.php`. No live MySQL test possible locally (no server/CLI/Docker); verified structurally via regex (0 inline `REFERENCES` in any CREATE; 8 FKs each backed by a Phase-1 table) + `git diff --check` + `check:docs`.

### cPanel Deployment Preparation Audit (2026-08-06)
- [x] **Deployment audit against the verified cPanel plan** (git clone to `/home/zizolear/public_html/tamasharoom`, doc root `.../tamasharoom/public`, domain `tamasharoom.ir`, MySQL production, private `local` subtitle disk via `storage/app/private`, `schedule:run` cron, no seeders, `.env`/vendor/node_modules/`public/build`-when-built-locally kept server-only).
- [x] **Verified locally (all pass, nothing blocked)** — `composer install` deps present (vendor tracked, `composer.lock` tracked, platform PHP 8.4.22); `npm run build` OK (10.3s, main chunk 130.7 kB gzip, `tsc` via `npm run build` = `tsc && vite build` per package.json); `php artisan config:cache` AND `php artisan route:cache` both succeed (routes cached to `bootstrap/cache/routes-v7.php`, verified working closures/`route:cache not blocked by closure route); `php artisan optimize:clear` restores clean cache. Frontend build output is gitignored (`/public/build`) so it must be uploaded separately if built locally. `.env.example` values confirmed: `SESSION_SECURE_COOKIE=true` present, `APP_ENV=local`, `APP_DEBUG=true`, `DB_CONNECTION=sqlite` (production overrides to `mysql`), `LOG_CHANNEL=stack` (production overrides to `daily`), `MAIL_MAILER=log` (email verification not enforced — `User` does not implement `MustVerifyEmail`), `SENTRY_DSN` empty → disabled.
- [x] **Added root `.htaccess (the only genuine in-scope change)** — repo deploys inside `public_html` (parent domain `zizolearn.ir` web root), so without repo-root `.htaccess` the parent domain could serve `.env`, `.git/`, `vendor/`, `storage/`, `composer.json`, etc. by URL traversal. New repo-root `.htaccess` blocks sensitive files/folders (FilesMatch + `mod_rewrite` `[F]`) without affecting `public/`, which remains served normally by untouched `public/.htaccess`). No app features, routes, schema, UI, CI, security (CSP/headers) hardening, storage:link, or architecture touched.
- [x] **No code blockers needing changes** — audit found no hardcoded filesystem paths in app code (only docs examples); no `storage:link` needed (private disk); PHP `public/` doc-root assumptions intact (`public/index.php`, `public/.htaccess` standard); `bootstrap/cache/.gitignore` = `*\n!.gitignore`; runtime caches gitignored. Production config per PROJECT.md/deployment-checklist stands.
- [x] **Frontend build strategy** — recommend **Option B (build locally/off-server, upload only `public/build/`)** as safest for this 2GB RAM / single-core cPanel (matches docs/deployment-checklist.md "Node is build-time only; cPanel needs only `public/build/`" and TASK.md §404 font-bundled claim); Node not required on cPanel. Option A (build on cPanel) possible but runs `tsc && vite build` on the constrained host with `npm ci` — heavier, no advantage here.

### Core Infrastructure
- [x] Laravel **13** application with Inertia + React (composer.json requires `laravel/framework: ^13.8`; AGENTS.md fixed from "Laravel 12")
- [x] Authentication (login, register, password reset)
- [x] Email verification routes/controllers exist but are **not enforced** — `User` does not implement `MustVerifyEmail` (commented out), so the `verified` middleware is a no-op; `MAIL_MAILER=log` in `.env`. See 4.4 note below.
- [x] Profile management (edit, update, destroy)

### Rooms
- [x] Create room with name and max members
- [x] Join room via invite code
- [x] Show room (dashboard listing)
- [x] Delete room (owner only)
- [x] **Rename room** (owner only, via settings dialog)
- [x] **Lock/unlock room** (prevents new joins, owner only)
- [x] **Regenerate invite code** (owner only)
- [x] **Kick member** (owner only, with confirmation)
- [x] **Transfer ownership** (owner only, with confirmation)
- [x] **Room Settings dialog** (rename, lock, invite management)
- [x] **Host controls in Member List** (kick/transfer buttons on hover)
- [x] **Confirmation dialogs** for destructive actions
- [x] **Toast notifications** for user feedback
- [x] Permission enforcement through RoomPolicy

### Playback Sync
- [x] Video URL setting (owner only)
- [x] Play/pause sync with host authority
- [x] Position sync with drift detection
- [x] State versioning with atomic increments
- [x] Playback rate support
- [x] Polling-based sync (3s interval)
- [x] Server timestamp for interpolation
- [x] Video proxy streaming with Range request support

### Chat
- [x] Send messages (polling, 3s)
- [x] Delete own messages — **Backend + frontend complete** (`ChatController@destroy`, `ChatMessagePolicy@delete`, delete button in `RoomChat` with confirm dialog)
- [x] Real-time polling

### Subtitles
- [x] Upload SRT and VTT files
- [x] Server-side SRT→VTT conversion
- [x] Multiple tracks per room
- [x] Active track persisted in localStorage
- [x] Subtitle overlay with cue rendering
- [x] Subtitle settings (bg opacity, position, RTL support)
- [x] Error and loading states
- [x] Guest browse/select of tracks without owner authority
- [x] Room-default subtitle track (owner-set, inherited by members)
- [x] Per-user local override of the inherited default + local time offset — Phase 13 (2026-08-03)

### Presence & Heartbeat
- [x] Online/offline detection via heartbeat (30s interval)
- [x] Socket-independent page unload detection (sendBeacon)
- [x] Timeout detection (90s threshold, scheduled every minute)
- [x] Reconnect with exponential backoff (30s → 60s → … → 5min max)
- [x] Presence-aware member list (green/gray dots, last seen)
- [x] Connection indicator (Wifi/WifiOff)
- [x] Owner crown icon
- [x] Polling-based presence list (5s)
- [x] Join/leave system messages in chat (O4, Phase 14 2026-08-03)

### Scheduled Tasks
- [x] `presence:timeout` — mark stale members offline every minute
- [x] `rooms:prune-inactive` — delete rooms inactive 7+ days
- [x] Queue worker — process jobs one batch at a time

## Testing
- [x] **274** PHPUnit tests passing (2086 assertions) — verified by runtime run on 2026-08-13: `php artisan test` 274/274. No data providers — static count equals runtime count. (Canonical count; skills reference `docs/TASK.md` rather than hardcoding it.)
- [x] **235** Frontend Vitest tests passing — verified by runtime run on 2026-08-13: `npm run test` 235/235. No parameterized tests — static count equals runtime count.
- [x] **24** Playwright E2E tests passing — verified by runtime run on 2026-08-12: `npm run test:e2e` 24/24 (chat 3, keyboard-a11y 1, lock-kick-transfer 4, multi-tab-reconnect 2, playback-sync-verification 4, presence-moments 1, room 3, subtitle 4, tap-to-play 2).
- [x] **19** axe accessibility tests passing — verified 2026-08-12: `npm run test:a11y` 19/19 (a11y 3, auth-a11y 6, contrast-a11y 8, room-a11y 1, welcome-a11y 1).
- [x] Build verification (tsc + vite), `npm run check:docs`, Prettier `format:check`, Pint `--test`, `git diff --check` — all clean 2026-08-12.

### Security Hardening
- [x] **SSRF Protection** — `UrlSecurityService` with DNS resolution, private IP blocking (RFC 1918, loopback, link-local, CGNAT), localhost hostname blocking, DNS rebinding protection
- [x] **File Upload Hardening** — MIME content verification in `UploadSubtitleRequest` (`after()` validation hook), format detection (SRT: numeric first line, VTT: WEBVTT header), rejects renamed executables and script injection
- [x] **Security Headers Middleware** — `Content-Security-Policy` (restrictive), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (all disabled), `Strict-Transport-Security` (production only), removed `X-Powered-By` and `Server` headers
- [x] **Rate Limiting** — Login (5/min per email+IP), Register (5/min per IP), Forgot-password (5/min per email+IP), Reset-password (5/min per IP), Chat (30/min), Playback (60/min), Video Proxy (30/min), Presence (60/min) via named rate limiters in `AppServiceProvider`
- [x] **Auth Audit** — `PlaybackController::update` and `setVideo` now use `$this->authorize()` with existing policies instead of manual user ID checks
- [x] **Info Leakage** — Production error handler returns `{'message': 'Server Error'}` for non-HTTP exceptions, debug info hidden when `APP_DEBUG=false`
- [x] **Comprehensive Tests** — `SecurityTest` (18 feature tests: headers, MIME validation, auth consistency, access control, info leakage), `UrlSecurityServiceTest` (15 unit tests: SSRF scenarios including IP ranges, hostname patterns, URL formats)

### Audit Fixes (2026-07-21)
- [x] **Critical: DeleteRoomAction wrapped in DB::transaction** — disk file deletion stays outside; DB rows are atomic
- [x] **Critical: Prune query no longer deletes freshly-created rooms** — `store()` sets `last_activity_at = now()`; `orWhereNull` removed from prune query
- [x] **Index on rooms.last_activity_at** — new migration, mitigates cap-check and prune query scans
- [x] **PlaybackMode backed enum** — `App\Enums\PlaybackMode` with `Direct`/`Proxy` cases; Room model cast; Action return type; TS union stays synchronized
- [x] **RTL fix** — `mr-auto` → `me-auto` in member-list.tsx
- [x] **Join rate limiter** — 10/min per user/IP on the join route
- [x] **Persian error messages** — room cap ("سرور در حال حاضر ظرفیت کامل دارد"), join rejection via `Response::deny()` ("این اتاق پر است", "این اتاق قفل شده است")
- [x] **Accessibility** — `aria-label` on toast dismiss button; removed redundant `useEffect` focus (now uses native `autoFocus`)
- [x] **Test stability** — `Carbon::setTestNow()` in time-sensitive prune tests
- [x] **Race condition coverage** — atomic join path test verifying `lockForUpdate()` guard
- [x] **Cleanup** — deleted stray root-level `TASK.md`; `TAMASHAROOM_MAX_CONCURRENT_ROOMS` added to `.env.example`; `docs/PROJECT.md` "Current Status" updated

### Batch 1 Production Fixes (2026-08-01)
- [x] **Issue 1 — Playback polling stops after first fetch** — `use-playback-sync.ts` only scheduled the poll once from the mount effect; `fetchState` never rescheduled. Rewrote with `fetchStateRef`/`cancelledRef`, `schedulePoll()` now re-scheduled in `finally` after every fetch (success or error), interval switches between `POLL_ACTIVE` (3s, playing) and `POLL_IDLE` (10s, paused), unmount cancels cleanly. `sync` now applies `playback_mode` from the PATCH response (was ignored). Added 6 Vitest cases; all 11 hook tests pass.
- [x] **Issue 2/3 — Proxy redirect SSRF + TLS verification** — `VideoProxyService` previously followed redirects automatically (`follow_location=1`) so `UrlSecurityService` only validated the *original* host, and all three stream contexts disabled TLS verification. Rewrote `fetchHead()` as a manual redirect loop (`MAX_REDIRECTS=5`, per-hop `validateVideoUrl`, loop detection, relative-URL resolution) and added `createStreamContext()` with `follow_location=0`/`max_redirects=0` + `verify_peer=true`/`verify_peer_name=true` for range/full requests. Removed the old SSL-disabled rationale comments. Added 6 tests; all 18 `VideoStreamTest` pass.
- [x] **Issue 4 — Playback update accepts unvalidated `video_url`** — `PlaybackController::update` passed `video_url` straight to `updatePlaybackState` with no SSRF check and no mode recompute. Now injects `UrlSecurityService` + `DetermineVideoPlaybackModeAction`, validates changed URLs (422 on failure), recomputes `playback_mode` only when the URL actually changed (avoids a HEAD per 3s poll), and returns `playback_mode` in the response. Added 5 tests; all 21 `PlaybackSyncTest` pass.
- [x] **Issue 5 — Room activity tracking missing** — chat, playback, presence heartbeat, and join never updated `last_activity_at`. Added `Room::touchActivityIfStale()` (throttled, 300s default) used by chat store + presence heartbeat; join calls `touchActivity()` in-transaction; `updatePlaybackState` now sets `last_activity_at = now()` in the same UPDATE. Added 4 tests (join/chat/heartbeat touch + playback assertion).
- [x] **Issue 6 — Room-cap race (count-then-insert)** — `StoreRoomRequest` counted active rooms then the controller inserted outside any lock; two concurrent creates could both pass the check. `RoomController::store` now acquires `Cache::lock('room-cap', 10)` around count+create (works with array + database cache stores). Added `room_cap_race_is_prevented_by_lock` regression test.
- [x] **Issue 7 — Subtitle files orphaned on account deletion** — `ProfileController::destroy` deleted the user (rooms cascade via FK) but subtitle files stayed on disk. Extracted `DeleteRoomAction::deleteFilesForOwnedRooms()` and call it before `$user->delete()`. Added regression test verifying the file is gone from `Storage::disk('public')`.

### Documentation & Dependency Cleanup (2026-07-22)
- [x] **AGENTS.md** — Radix → Headless UI, Pest → PHPUnit in Stack and Commands
- [x] **PROJECT.md** — same Radix/Pest fixes across tech table, directory tree, scripts, and changelog; added 2026-07-22 changelog entry
- [x] **SYSTEM.md** — Radix → Headless UI in component guide, focus trap examples, file structure comment, and primitive rule
- [x] **TASK.md** — Pest → PHPUnit in CI description; CI runs updated
- [x] **composer.json** — removed stale `pestphp/pest-plugin` from `allow-plugins`
- [x] **package.json** — removed unused `zod-validation-error`; added `zod`, `zustand`, `vitest`, `prettier`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`; added `type-check`, `format`, `format:check`, `test`, `test:watch`, `test:e2e` scripts
- [x] **laravel/sanctum** — installed, config published, migration run
- [x] **vitest.config.ts** — created with jsdom, `@/` alias, React plugin, setup file
- [x] **tests/e2e/playwright.config.ts** — created scaffolding (mirrors a11y config)
- [x] **Frontend tests** — 15 unit tests for `computeExpectedPosition`, `toPlaybackState`, and `usePlaybackSync` (drift detection, state versioning, debounce, error handling, host authority, `onRemoteChange` callback)

### Zustand Store Migration (2026-07-22)
- [x] **Zustand stores created** — `resources/js/stores/theme.ts`, `room-ui.ts`, `subtitle.ts`
- [x] **Theme store** — dark mode toggle; replaces `useState<boolean>` in AppLayout.tsx
- [x] **Room UI store** — active tab, modal visibilities, video URL, room name, invite code, is_locked; replaces 14 `useState` calls in Pages/Rooms/Show.tsx
- [x] **Subtitle store** — subtitle settings with localStorage persistence; replaces `useSubtitleSettings` hook in subtitle-overlay.tsx
- [x] **All components wired** — AppLayout, Rooms/Show, subtitle-overlay, subtitle-settings consume Zustand stores directly
- [x] **Lint pass** — ESLint warnings reduced from 5→3; Show.tsx useEffect deps completed; unused `SubtitlePosition` import removed

### Playwright E2E & A11y Tests (2026-07-22)
- [x] **Test helper routes** — `routes/test-helpers.php` loaded in `local`/`testing` env; provides `POST /__test/setup-verified-room` (creates user + room, logs in) and `POST /__test/join-room` (creates verified member in existing room)
- [x] **A11y test for room page** — `tests/a11y/room-a11y.spec.ts` navigates to a room and runs axe (wcag2a/wcag2aa/wcag21a/wcag21aa/best-practice); filters critical+serious violations
- [x] **E2E tests** — `tests/e2e/room.spec.ts`:
  - Host can create a room and see the room page
  - Guest joins room via invite code (two browser contexts)
  - Playback state propagates from host to guest (set video URL + patch is_playing → poll until guest sees it)
- [x] **Playwright configs cross-platform** — removed `channel: "chrome"` from both a11y and e2e configs; uses bundled Chromium

### CI Pipeline Update (2026-07-22)
- [x] **`.github/workflows/ci.yml`** — added steps:
  - `npm run test` (Vitest frontend unit tests)
  - `npx playwright install chromium` (install test browser)
  - `npm run test:a11y` (Playwright a11y, `APP_ENV=local`)
  - `npm run test:e2e` (Playwright E2E, `APP_ENV=local`)

### CI Fix: Missing APP_KEY (2026-07-25)
- [x] **`.github/workflows/ci.yml`** — added `php artisan key:generate` step after environment file creation, before running any tests or PHP commands that require an application key

### A11y Fixes — First Green Run (2026-07-25)
- [x] **AppLayout logout nested-interactive** — removed `<Link as="button">` wrapper, used `router.post()` directly on `<Button>` with `aria-label="خروج"`
- [x] **AuthenticatedLayout logo aria-label** — added `aria-label="خانه"` to home/logo `<Link>`
- [x] **Dashboard card copy-link contrast (1.79:1)** — `text-primary` → `text-muted-foreground hover:text-primary`
- [x] **Tab nav contrast (3.89:1 → ~6:1)** — darkened light-mode `--muted-foreground` from `30,8%,40%` to `30,12%,26%`
- [x] **Chat timestamp opacity** — replaced `opacity-60` with conditional `text-primary-foreground/80` / `text-secondary-foreground/80`
- [x] **Toast close button, track extension label** — `opacity-60` → `opacity-80` on remaining text elements

### E2E All Green — Serializer, Test Helper & Route Binding Fixes (2026-07-25)
- [x] **`is_owner` serializer** — added `$appends = ['is_owner']` + accessor to `RoomMember` model; updated `RoomController::members()` to set `room` relation on each loaded member; hidden `room` from serialization
- [x] **FRONTEND_CONTRACT.md updated** — `RoomMember` interface now documents `is_owner: boolean` (was a docs-vs-reality gap: `PresenceMember` had it but `RoomMember` did not)
- [x] **`__test/join-room`** — changed to log in as the room's current owner if that owner is already a member, instead of always creating a new user. Fixes ownership-transfer test: after transfer, the new owner (original guest) is correctly returned.
- [x] **Kick/transfer route binding** — changed `User $target` to `int $targetId` in both `RoomController::kick()` and `transfer()`. Implicit model binding threw 404 before policy evaluation; now the controller checks authorization first and returns proper 403/404 responses.
- [x] **E2E results**: 12/12 pass (chat 2/2, lock-kick-transfer 4/4, room 3/3, subtitle 3/3). PHPUnit 166/166. Lint + TypeScript + Pint all pass.

### E2E CSRF Fix & SRT MIME Validation Fix (2026-07-25)
- [x] **CSRF root cause**: `page.request.post()` in Playwright sends cookies differently than a real browser, breaking the encrypted `X-XSRF-TOKEN` header flow used by `PreventRequestForgery::getTokenFromRequest()`
- [x] **Fix applied**: `bootstrap/app.php` — `encryptCookies(except: ['XSRF-TOKEN'])` stores the cookie as plaintext; all E2E tests send `_token` form field instead of `X-XSRF-TOKEN` header
- [x] **Playwright baseURL bug**: `browser.newContext()` does NOT inherit `baseURL` from config — fixed by passing `{ baseURL: "http://127.0.0.1:8000" }` to all `newContext()` calls
- [x] **SRT MIME validation fix**: `mimes:srt,vtt` in `UploadSubtitleRequest` uses Symfony's `guessExtension()` on file **content**, not the filename. SRT content (starting with sequence number `1`) is guessed as extension `txt`, so `mimes:srt,vtt` fails for all SRT files. Changed to `mimes:srt,vtt,txt` — added `txt` since that's what Symfony's guesser returns for SRT content. The `after()` hook still validates actual content format, rejecting non-subtitle text files.
- [x] **E2E results**: 9/12 pass (chat 2/2, room 3/3, subtitle 3/3, lock-kick-transfer 1/4). Backend: 166/166 pass. Lint + TypeScript + Pint all pass.
- [x] **3 remaining failures diagnosed** (lock-kick-transfer): all caused by missing `is_owner` field in member API response — see Pending section

### Documentation & Security Cleanup (2026-07-22)
- [x] **PROJECT.md tech stack** — Vite 6→5, Zod 3→4, Vitest 2→3; React Query line removed (not installed); removed `test:ui` script (doesn't exist in package.json)
- [x] **PROJECT.md SYSTEM.docx references** — all 9 occurrences changed to `SYSTEM.md`
- [x] **PROJECT.md directory structure** — rebuilt from actual disk listing: added `Actions/`, `Enums/`, `Providers/`, `Services/`, `Hooks/` (uppercase), `types/`, `__tests__/`, `Pages/Auth/`, `Pages/Profile/`; removed `Api/`, `Resources/`, `layout/`, `images/`; fixed `hooks/`→`Hooks/` (case-sensitive Linux compat); added actual file listings
- [x] **PROJECT.md public/ section** — removed `images/` and `sitemap.xml` (don't exist on disk)
- [x] **`.opencode/skills/component-architecture/SKILL.md:42`** — Radix→Headless UI
- [x] **`.opencode/skills/component-architecture/SKILL.md` CVA section** — replaced with `cn()` pattern (matches actual usage — `clsx` is installed, CVA is not)
- [x] **SYSTEM.md section 15.05** — entire CVA section replaced with `cn()`/`clsx`-based variant guidance; all CVA import examples removed
- [x] **SYSTEM.md button template** — replaced CVA-based example with `cn()`-based equivalent (variant classes via `Record<string, string>`)
- [x] **`README.md`** — replaced default Laravel scaffolding with TamashaRoom overview, setup instructions, and quality commands
- [x] **`VideoProxyService`** — added SSL verification disable rationale comment to all three `stream_context_create` blocks (`fetchHead`, `handleRangeRequest`, `handleFullRequest`)
- [x] **`docs/TASK.md`** — marked all completed items; added SSRF TOCTOU entry to Accepted MVP Limitations; updated test counts; updated Chat section (delete button now complete); removed completed items from Future Features

### Comprehensive Audit (2026-07-22)
- [x] **Feature completeness verified** — 44 of 46 items confirmed implemented end-to-end. 2 partial items documented below.
- [x] **Test coverage surveyed** — 116 Feature tests + 32 Unit tests (both **~3x higher** than documented). 15 Vitest tests match. E2E and a11y tests exist but are thin; chat has zero tests at any layer.
- [x] **Documentation inconsistencies found and catalogued** — 20+ issues across PROJECT.md, SYSTEM.md, AGENTS.md, and the skill file.
- [x] **Security posture verified** — All 7 hardening items confirmed intact. SSRF protects all 5 RFC 1918 ranges + CGNAT + IPv6. CSP has nonce-based production mode. CSRF exception scoped strictly to `__test/*`. Test-helper routes double-gated (file-load + handler-level). Minor note on VideoProxyService SSL verification disabled.
- [x] **Deployment readiness assessed** — No production steps have been executed. The then-current deployment plan listed four items (migrations, storage:link, queue worker, cron); the current subtitle implementation uses the private `local` disk, so `storage:link` is no longer a prerequisite.
- [x] **Tech debt and gaps catalogued** — See Pending and Future Features sections below.

### Items That Need Attention (from audit)

#### Features
- [x] **Delete own messages — no UI** (`room-chat.tsx` lacked delete button; added with ConfirmDialog)
- [x] **Frontend component tests** — 64 tests added across 6 files (room-chat, member-list, video-player, subtitle-overlay, subtitle-settings, subtitle-parser)
- [x] **Rate limiting** — 7 tests exercising all 5 rate limiters (login, chat, playback, proxy, presence)
- [x] **VideoProxyService SSL verification disabled** — documented with rationale comment on all three stream contexts
- [x] **Dashboard a11y test un-skipped** — now uses `GET /__test/setup-verified-room` to create a verified user and login, bypassing the email-verification redirect
- [x] **SRT format detection tightened** — now validates the SRT timing line format (`HH:MM:SS,mmm --> HH:MM:SS,mmm`) on the second non-empty line
#### Documentation vs. Code Mismatches
- [x] **PROJECT.md tech stack** — Vite 6→5, Zod 3→4, Vitest 2→3; React Query removed (not installed)
- [x] **PROJECT.md** — nine `SYSTEM.docx` references changed to `SYSTEM.md`
- [x] **PROJECT.md directory structure** — aligned with actual disk (added Actions, Enums, Providers, Services, Hooks, types, __tests__; removed Api/, Resources/, layout/, images/; fixed `hooks/`→`Hooks/`)
- [x] **`.opencode/skills/component-architecture/SKILL.md`** — Radix→Headless UI; CVA→`cn()` pattern
- [x] **SYSTEM.md section 15.05** — replaced CVA examples with `cn()`/`clsx` pattern; removed `class-variance-authority` import
- [x] **`README.md`** — replaced default Laravel scaffolding with TamashaRoom project description
- [x] **`VideoProxyService`** — added SSL verification disable rationale comment to all three stream contexts
- [x] **`.env.example`** — added `SESSION_SECURE_COOKIE=true`, changed `APP_LOCALE=en`→`fa`, changed `APP_NAME=Laravel`→`TamashaRoom`
- [x] **`resources/js/Hooks/` casing** — directory is `Hooks/` (uppercase), PROJECT.md already shows `Hooks/`; no lowercase `hooks/` directory exists separately
- [x] **`docs/deployment-checklist.md`** — aligned with codebase: `CACHE_DRIVER`→`CACHE_STORE=database` (Laravel 13 var; obsolete name removed repo-wide), `SESSION_DRIVER=database` (was "file or database"), corrected the queue-worker section (was persistent python wrapper + VPS systemd — replaced with scheduler-batch drain per `routes/console.php`), fixed the migration composition list (now 14: 3 framework base + 11 application), corrected `npm ci --production`→`npm ci`, added `composer install --no-dev --optimize-autoloader`, documented `public/` web root + `public/build` assets, corrected the `queue:status` verify step
- [x] **`docs/deployment-checklist.md` (Batch 2A.1 verification fixes)** — Section 5 now verifies all 3 scheduled tasks (presence:timeout, rooms:prune-inactive --days=7, queue:work --stop-when-empty --max-time=30); `key:generate --force` made explicitly conditional (new install only, never regen existing production APP_KEY); generic `migrate:rollback` guidance replaced with restore-backup-or-reviewed-corrective-migration; Node build workflow clarified as off-server buildable (cPanel only needs `public/build/`)
- [x] **`useRoomOwnership` hook (Batch 2C, TAM-005)** — ownership was computed from the immutable `room.user_id` Inertia prop in `Show.tsx`, so after a transfer the old owner kept owner-only controls and the new owner never gained them until reload. Added `resources/js/Hooks/use-room-ownership.ts`: `isOwner`/`ownerId` derive from the reactive `room-ui` store `ownerId` (with prop fallback), presence data (which carries `is_owner` from `PresenceService`) syncs the new owner in, a 10s cooldown prevents a stale presence poll from reverting a just-performed transfer, and `transferOwnership` updates the store. `Show.tsx` now uses the hook (controls at `canControl`/set-video/subtitle gate on the reactive value). 5 hook tests + 1 member-list failure-path test (failed transfer never calls `onTransfer`)

#### Test Coverage Gaps
- [x] **Chat endpoints** — 10 Feature tests added (send, list, delete, auth, validation)
- [x] **Frontend components** — 64 component tests added across 6 files
- [x] **Rate limiting** — 7 Feature tests added (login, chat, playback, proxy, presence)
- [x] **Zustand stores** — 23 tests added: theme (5), room-ui (11), subtitle (7)
- [x] **Dashboard a11y test** — un-skipped (uses `__test/setup-verified-room` helper)
- [x] **Auth/profile a11y (Batch 2D, TAM-004)** — axe coverage added for `/reset-password/{token}`, `/confirm-password`, and the Profile delete-account modal in its open state; login, register, forgot-password, verify-email, and Profile default state were already covered by `tests/a11y/a11y.spec.ts` + `tests/a11y/auth-a11y.spec.ts`
- [x] **E2E tests** — 12 of 12 pass (chat 2/2, lock-kick-transfer 4/4, room 3/3, subtitle 3/3)
- [x] **Subtitle content sanitization (Batch 2B, TAM-003)** — regression coverage added: backend `extractCues` strips script/`onerror` payloads (unit), end-to-end upload→`/cues` sanitization + `text/vtt` content-type (feature), frontend `parseVtt`/`parseSrt` strip script/img/javascript: payloads (parser), and overlay renders script-like cues as escaped text with no executable DOM element (overlay render). Security boundary confirmed safe — no behavior change

### Production & Security Guidance Correction (Batch G1, 2026-08-01) — documentation only
- [x] **Scheduler docs aligned with `routes/console.php`** — `docs/SYSTEM.md` 18.07 code block, 18.11 checklist, and the deployment-checklist skill now show exactly the 3 real scheduled tasks (rooms:prune-inactive --days=7 daily, queue:work --stop-when-empty --max-time=30 every minute, presence:timeout every minute); obsolete `sitemap:generate` / `sessions:prune` / `queue:work --max-time=50` removed.
- [x] **Rollback guidance corrected** — deployment-checklist skill now says restore the pre-deploy database backup or apply a specifically reviewed corrective migration; never `migrate:rollback` against production.
- [x] **`docs/ai/` reconciled with reality** — PROJECT_BASELINE: 14 migrations (3 framework base + 11 application), queue worker confirmed as scheduler batch drain (no python wrapper / persistent worker), a11y 11/11 confirmed, coverage-gap section updated; ISSUE_REGISTER: TAM-002/003/004/005 marked RESOLVED (Batches 2A/2B/2D/2C), TAM-200 (a11y count) verified/resolved, Recommended Fix Order trimmed.
- [x] **Rate-limiting claim corrected (document only; no code change)** — SYSTEM.md 18.08 Rule 5 + 18.11 checklist, security-rules skill, ARCHITECTURE.md, and ENGINEERING_GUARDRAILS.md now state the true current state: login/chat/playback/proxy/presence/join/email-verification throttled; `POST /register`, `POST /forgot-password`, `POST /reset-password` NOT yet throttled (known gap to close before launch).
- [x] **Obsolete sitemap assumptions removed** — SYSTEM.md 18.06 Rule 2, 23.04 requirement 3, 23.06 checklist, and laravel-backend-rules skill no longer claim a generated sitemap (no `sitemap:generate` command exists; robots.txt is static).
- [x] **TASK.md stale checkboxes fixed** — Batch 2B subtitle sanitization and E2E 12/12 marked done.

### Frontend/Backend Architecture Guidance Correction (Batch G2, 2026-08-01) — documentation only
- [x] **Absolute "no client fetching" rule corrected across 8 files** — the blanket "pages never fetch their own data on mount" claim now has the approved polling carve-out (playback state, presence, chat via axios `api` client against session-auth JSON endpoints in `routes/web.php`), applied to AGENTS.md, FRONTEND_CONTRACT.md intro, SYSTEM.md 18.02 Rule 1, 18.11 checklist, 27.10 (both checklists), laravel-backend-rules skill, anti-patterns skill, ARCHITECTURE.md.
- [x] **"Every mutation via Form Request / useForm" absolutes corrected** — structured input → Form Request; simple single-field action endpoints → inline `$request->validate()` (e.g. `ChatController::store`); Inertia forms → `useForm`; JSON action endpoints → axios `api` with local pending state (room-settings `api.patch`, chat send). Updated in SYSTEM.md 16.05, 18.04 Rule 1/2, 18.08 Rule 1/3, 18.11, 24.05, 27.10, laravel-backend-rules, security-rules, ARCHITECTURE.md §2/§6.
- [x] **4-way endpoint taxonomy documented** — Inertia page props / JSON polling endpoints / JSON action endpoints (all `routes/web.php`, session guard) / external Sanctum `routes/api.php` (currently only `GET /user`). See laravel-backend-rules Structure section, security-rules rule 1, ARCHITECTURE.md §6.
- [x] **`usePollingReload` doc corrected to actual source** — SYSTEM.md 18.05 Rule 2 example now matches `usePollingReload(intervalMs = 5000)` + `router.reload()` (no `{ only }`), and notes production polling uses the axios `api` client, not this unused utility; ARCHITECTURE.md §4 updated.
- [x] **Fictitious examples replaced** — SYSTEM.md 18.02/18.04 replaced `Projects/Index`/`ProjectController`/`StoreProjectRequest` examples with the actual `RoomController::index`/`StoreRoomRequest`; 24.05 removed nonexistent `FormField.tsx` in favor of the real `InputError` component.
- [x] **FRONTEND_CONTRACT.md `chatMessages` → `chat_messages`** — Eloquent serializes the `chatMessages` relation as snake_case (`chat_messages`), matching `Show.tsx`'s `room.chat_messages`; §1.1 and §3.1 corrected.

### Repository Structure & Naming Alignment (Batch G3, 2026-08-01) — documentation only
- [x] **PROJECT.md directory tree aligned with the actual repo** — `PresenceTimeout.php` → `MarkStaleMembersOffline.php` (`# signature: presence:timeout`, matches `routes/console.php`); removed nonexistent `RoomMemberPolicy` / `SubtitleTrackPolicy` (only `ChatMessagePolicy` + `RoomPolicy` exist); added missing real files: `AdminController`, `JoinRoomRequest`/`ProfileUpdateRequest`/`UpdateRoomRequest`/`UploadSubtitleRequest`, `MemberPresenceChanged`/`NewChatMessage` events, `use-room-ownership`, `AuthenticatedLayout`/`GuestLayout`, `lib/api.ts`, `vite-env.d.ts`, and the real route files (`auth.php`, `channels.php`, `test-helpers.php`).
- [x] **Subtitle parser location corrected** — PROJECT.md listed a nonexistent `lib/subtitle-parser.ts`; SRT/VTT parsing actually lives in `Components/composite/subtitle-overlay.tsx` (`parseVtt`/`parseSrt`/`parseSubtitle`), and `lib/types/` holds plain TS types (not Zod schemas).
- [x] **Hooks/Layouts case corrected** — SYSTEM.md 18.05 rule comments and 28.05 import-order example, laravel-backend-rules skill, and output-conventions skill now use `resources/js/Hooks/` and `@/Components`, `@/Hooks`, `@/lib`, `@/stores` (uppercase dirs, real imports).
- [x] **React Query examples removed** — output-conventions skill no longer shows `@tanstack/react-query` (not installed); example imports replaced with real deps (`zustand`) and real hooks (`usePresence`, `useToast`).
- [x] **SYSTEM.md 15.03 File Organization tree + File Location Rule aligned** — dropped the generic `Components/layout/`, `Components/providers/`, `features/[feature]/components/` (none exist); layouts are in `resources/js/Layouts/`, composites are the real `room-chat`/`member-list`/`video-player`/`subtitle-overlay`/etc.; `Components/shared/` ref in 16.02 replaced with the real `Components/composite/`.

### Deployment & Environment Consistency (Batch G4, 2026-08-02) — documentation only
- [x] **Unconfirmed production domain replaced with placeholder** — `docs/PROJECT.md` `APP_URL=https://tamasharoom.app` and `docs/SYSTEM.md` API-client example `https://tamasharoom.app/api/v1` changed to `https://yourdomain.com` (the same placeholder `docs/deployment-checklist.md` already uses). The repository has not finalized a production domain: README claims `tamasharoom.ir`, `.env.example` ships `APP_URL=http://localhost`, PROJECT/SYSTEM previously claimed `tamasharoom.app`. Real domain to be set when registered.
- [x] **Node/cPanel build claim reconciled** — `docs/SYSTEM.md` 18.00 claimed "Node.js 22 is available on the host"; corrected to match `docs/deployment-checklist.md`: Node is a build-time tool only, the build may be run off-server (any machine with Node 22+), and cPanel needs only `public/build/`. The deployment-checklist skill's step 3 now states the off-server option explicitly.
- [x] **CI checks documented accurately** — `docs/SYSTEM.md` 29.02 "CI passes" now lists the full actual suite (build, lint, Pint, type-check, PHPUnit, Vitest, Playwright a11y + E2E), and the deployment-checklist skill's pre-deploy CI line includes Pint + Playwright (both run in `.github/workflows/ci.yml`).
- [x] **Prettier not gated in CI documented** — `npm run format:check` exists in `package.json` but CI has no Prettier step; noted in `docs/SYSTEM.md` 29.02 and the deployment-checklist skill so nobody assumes CI enforces formatting.
- [x] **ESLint warning threshold documented** — `package.json` lint script runs `eslint resources/js --max-warnings 4`; `docs/SYSTEM.md` 29.02 now states "zero errors and at most 4 warnings".
- [x] **PROJECT.md env block completed** — added `TAMASHAROOM_MAX_CONCURRENT_ROOMS=50` (from `config/tamasharoom.php`) and `SENTRY_DSN=` (from `config/sentry.php`); both already present in `.env.example`.
- [x] **Unresolved (documented only, NOT changed)** — `composer.json` is still template identity (`name: laravel/laravel`, `description: "The skeleton application for the Laravel framework."`, `license: MIT`) which conflicts with README's proprietary-notice; per Batch G4 rules legal/license metadata was not touched. Actual production domain still unfinalized. `public/build` is gitignored, so the off-server build must be uploaded manually.

### Final Documentation Consistency Cleanup (Batch G8, 2026-08-02) — documentation only
- [x] **Independent source-level test recount (methodology)** — PHPUnit: counted `#[Test]` attributes + `public function test_*` methods in every `tests/Feature/**/*.php` and `tests/Unit/**/*.php` (recursive); **194** static declarations (160 Feature + 34 Unit, incl. the 18-test `tests/Feature/Auth/` suite) = the 194 `php artisan test` runtime count (no data providers). Vitest: counted `it(`/`test(` declarations in all `resources/js/__tests__/*.test.ts(x)`; **122** static (99 non-store + 23 Zustand store: theme 5, room-ui 11, subtitle 7) = the 122 `npm run test` runtime count (no `it.each`/`test.each`). Playwright E2E **12** passed; axe **11** passed. No runtime-vs-static divergence anywhere — all previous figures that claimed different counts (183/151+32, 107/84+23, 136) were stale and corrected.
- [x] **`docs/TASK.md` Testing section synchronized** — the canonical counts now read 194 (160 Feature + 34 Unit), 122 Vitest (99 + 23 store), 12 E2E, 11 a11y, each with the recount date and static-vs-runtime note. Historical counts remain in their original dated entries and were not rewritten.
- [x] **`docs/PROJECT.md`** — Zustand row corrected from "theme, sidebar, modals" to the real stores (`theme`, `room-ui`, `subtitle`) with responsibilities; Architecture Principles now state the approved live-room Axios polling/action exception, clarify Form Requests (structured input) vs inline `$request->validate()` (simple action endpoints), and reframe "one controller / one Form Request / one Inertia page per resource" as a preferred organization pattern rather than a hard rule; Inertia versions documented accurately (`@inertiajs/react` 2.x client, `inertiajs/inertia-laravel` 3.1.x server); the authoritative design-system document is now identified as `design-systems/tamasharoom/DESIGN.md` (draft) — no stale `DESIGN.md` path reference existed in PROJECT.md itself; document metadata date updated to 2026-08-02.
- [x] **`FRONTEND_CONTRACT.md`** — subtitle behavior corrected: supported filename extensions are only `.srt` and `.vtt`; `txt` is solely a MIME-detection fallback (Symfony guesses SRT content as `txt`); a `.txt` filename itself is rejected by the post-validation extension check. POST auth-route naming claims corrected (unnamed POST routes do not inherit names from GET routes; the frontend posts to the URL generated by the named GET route, e.g. `route('login')`). Inertia client/server adapter versions clarified. Axios/live-room polling contract preserved unchanged.
- [x] **`docs/ai/PROJECT_BASELINE.md`** — test numbers recounted with the same source-level methodology (Backend 194 = 160 Feature + 34 Unit; Frontend 122 = 99 + 23 store; E2E 12; a11y 11); obsolete sidebar-store implication removed; room-ownership-transfer UX marked RESOLVED with historical context preserved.
- [x] **`docs/ai/ISSUE_REGISTER.md`** — TAM-003 wording reconciled: no claim that tests were executed beyond what was independently run this pass; historical verification distinguished from current independent verification.
- [x] **`.opencode/skills/testing-strategy/SKILL.md`** — baseline reconciled to the same verified counts (194 PHPUnit = 160 Feature + 34 Unit; 122 Vitest; 12 E2E; 11 a11y) with the static-vs-runtime (data-provider/parameterized) caveat preserved.
- [x] **`docs/SYSTEM.md`** — generic `fetch` guidance clarified to not override the shared Axios `api` client for live-room JSON polling/actions; overbroad API/Form Request/Policy wording narrowed to endpoints that accept input or access protected resources; Sanctum auth requirements kept accurate; no new architecture rule introduced.
- [x] **`PRODUCT.md`** — nonexistent `DESIGN.md` reference fixed: the file does not exist at that path; the authoritative design-system document is `design-systems/tamasharoom/DESIGN.md` (draft status).

### Authentication Rate-Limit Hardening (2026-08-02)
- [x] **Auth POST endpoints protected** — previously unthrottled `POST /register`, `POST /forgot-password`, and `POST /reset-password` now carry named Laravel rate limiters attached directly in `routes/auth.php` (same convention as the existing `throttle:login`).
- [x] **Limiter strategy** — three new named limiters in `app/Providers/AppServiceProvider.php`, all 5/min to match the existing login limiter: `register` (per-IP only), `forgot-password` (per-email+IP, mirrors `login`), `reset-password` (per-IP only). No new library, no DB/schema/dependency/deploy/frontend changes; existing `login`/`chat`/`playback`/`proxy`/`presence`/`join` limiters untouched.
- [x] **Tests** — 8 tests added to `tests/Feature/RateLimiterTest.php` (15 total in the class): each endpoint allows requests under the limit and returns 429 past it, scoping is proven per-IP (`register`, `reset-password`) and per-email (`forgot-password`), legitimate registration is allowed, and a route-attachment test asserts `throttle:*` middleware is actually bound to the intended POST routes (including `throttle:login` unchanged).
- [x] **Test results** — `php artisan test`: **202/202 passed** (194 baseline + 8 new); Auth filter: **23/23 passed**; RateLimiterTest: **15/15 passed**. `npm run type-check` and `npm run lint` clean (no frontend impact); `./vendor/bin/pint` clean on the 3 changed files.

### AI/Context Efficiency (Phase 1 & 2, 2026-08-02)
- [x] **Test-count reconciliation** — verified actual `php artisan test` = **214** (703 assertions) and static source count = 214 (173 Feature + 41 Unit). The skills' 214 was correct; `docs/TASK.md`'s "202" was stale. Corrected the Testing section to **214** and established `docs/TASK.md` as the **single canonical source** for test counts; skills reference it instead of hardcoding.
- [x] **`docs/SYSTEM.md` chapter→line index** — added a compact 29-chapter index at the top (verified all 29 ranges match actual `# NN` header positions). Skills referencing "SYSTEM.md Chapter NN" are now line-addressable via Read offset/limit.
- [x] **`docs/MAP.md` created** — lightweight navigation map: hierarchy, cross-cutting ref docs, and per-subsystem → skill/docs → controller/service → hook/component → test mappings (rooms, playback, chat, subtitles, presence, lifecycle, auth, security) + a test-location index. Points to files; never restates rules.
- [x] **`AGENTS.md` trimmed** — intro now points to `docs/MAP.md` and `docs/SYSTEM.md`'s chapter index; "Current Status" no longer hardcodes E2E/a11y counts (now says the canonical source is TASK.md). No non-negotiable rule removed.
- [x] **Skills de-staled** — removed hardcoded test counts from `testing-strategy` (now points to TASK.md) and `ai-efficiency` (removed the hardcoded PHPUnit/Vitest/E2E/a11y baseline, replaced with "see TASK.md"). Added a cross-reference from `laravel-backend-rules` endpoint categories to `security-rules`'s API-boundary section.
- [x] **`ai-efficiency` refined** — narrowed frontmatter description (no longer auto-activates for every task), added `docs/MAP.md` + SYSTEM.md chapter-index to progressive disclosure and exploration patterns, removed stale test-count example, encouraged `docs/MAP.md` + targeted source reads + compact task prompts.
- [x] **Skill-overlap review** — `react-rules`/`code-review-rules`, `laravel-backend-rules`/`security-rules`, `rtl-and-design-system`/`typescript-tailwind-rules`, `ai-efficiency`/`testing-strategy` were reviewed. Determined they are complementary lenses (architecture vs review vs watch), already cross-reference each other where overlap is real; no unique knowledge removed (per "don't remove useful knowledge just for line count").
- [x] **`docs/ai/` reviewed (NO/ACTION)** — `PROJECT_BASELINE.md` (343 lines) and `ARCHITECTURE.md` (474 lines) are level-4 **verification evidence** with confidence markers and inter-service flow analysis, not redundant restatement; they're not loaded in normal code flow. Converting to pointers would lose verified analysis, so left unchanged.
- [x] **Verification** — all 17 skill directories referenced in AGENTS.md table; no stale test counts remain anywhere in skills/AGENTS; SYSTEM.md index verified correct; `git diff` contains **docs/skills/context only** (no app code, tests, routes, migrations, or config).
- [x] **Phase 3 (committed 2026-08-02, `08e2520`)** — `ai-efficiency` gained Code Reading Order (function-before-file), Task-Scoping, objective Stop-Reading/Stop-Searching, Tool-Agnostic note, and reduced its duplicate test command matrix to a pointer; `testing-strategy` gained the Verification Escalation (Levels 1–4) model; `AGENTS.md` aligned the `ai-efficiency` trigger with its narrowed frontmatter.
- [x] **Phase 4 (committed `5cecbe1`)** — collapsed `ai-efficiency`'s inline "Targeted Test Execution" rows into a single pointer to `testing-strategy`'s canonical Levels 1–4 matrix (removed the 4-row dup table; `ai-efficiency` 211→202 lines, single owner of the command matrix). Verified all SYSTEM.md chapter refs across skills resolve; no app code touched.
- [x] **Phase 5 (this session)** — audited the full real workflow (task → exploration → implementation → tests → review → completion). Verified `docs/MAP.md` file paths + test-location index all resolve; no opencode.json/subagent config exists; all cross-skill refs valid; all SYSTEM.md chapter refs valid. One real redundancy fixed: `git-workflow` step 4 re-listed the verification command set that `testing-strategy` (Levels 1–4) and `AGENTS.md` (Commands) already own — replaced with a pointer to the canonical escalation matrix + AGENTS/pkg.json command source (3-way command list → single owner, one source of drift removed). No other high-ROI bottleneck found; skills/docs only.

### Quick Wins (Phase 6, 2026-08-02)
- [x] **Narrow ownership/member/chat user serialization** — `RoomController@index`/`@show`/`@update` now eager-load `owner:id,name` only; `@show` member + chat user relations load `user:id,name`; `@members` loads `user:id,name`. Full `User` models (email, created_at, etc.) are no longer exposed to room pages beyond the authed `auth.user` prop. `ChatController` already used this scope; `PresenceService` already narrows. Frontend type-check clean (only consumes `id`/`name`).
- [x] **Pause background polling while tab hidden** — `usePlaybackSync`, `usePresence`, and `RoomChat` now skip poll/heartbeat scheduling when `document.hidden`, and re-fire an immediate fetch on `visibilitychange` back to visible (playback + presence). No transport change; keeps the polling architecture.
- [x] **Delete dead `AdminController`** — no routes referenced it, it called nonexistent `User::isAdmin()` (would fatal) and its `pruneInactive` bypassed `DeleteRoomAction`. Removed the file and the `docs/PROJECT.md` tree entry. No admin system was ever routed.
- [x] **Tests** — 2 new backend tests (`RoomManagementTest`: members JSON excludes user email and is limited to `id`/`name`; room `show` page does not leak owner email). 2 new frontend tests (`usePlaybackSync`: no polls while the document is hidden; resumes polling when visible). **Results:** `php artisan test --filter=RoomManagementTest` 29/29; `usePlaybackSync` Vitest 18/18; `npm run type-check` clean; `./vendor/bin/pint` clean.

### Quick Wins (Phase 8, 2026-08-02) — backend refactor
- [x] **`CreateRoomAction` extracted** — room-create business logic moved out of `RoomController::store` into `app/Actions/CreateRoomAction.php`. The Action owns the cache-store guard (production + ArrayStore/NullStore → critical log + ValidationException), the `Cache::lock('room-cap', 10)` acquisition, the cap re-check inside the lock, room creation (setting `user_id` directly, no longer via `$fillable`), and the owner's `RoomMember` row (`presence_status=online`), releasing the lock in `finally`. Same Persian validation messages as before.
- [x] **Single-source room-cap logic** — new `Room::isAtActiveRoomCapacity()` is the one place the active-room count query + cap threshold live. Both `StoreRoomRequest::withValidator` and `CreateRoomAction` (re-checked inside its lock) call it; the threshold comes from `config('tamasharoom.max_concurrent_rooms', 50)`.
- [x] **`user_id` removed from `Room::$fillable`** — room ownership can no longer be mass-assigned. Factory bypasses `$fillable` (`Model::unguarded()`), so `Room::factory()` keeps working; direct assignments fixed in `RoomController::transfer`, `routes/test-helpers.php`, and the new Action.
- [x] **`POST /rooms` rate-limited** — new `room-create` named limiter (`Limit::perMinute(5)`, keyed by user id or IP) attached to the store route in `routes/web.php`.
- [x] **Deterministic video-proxy 206 test** — `test_video_proxy_returns_206_for_valid_range` no longer hits the network. `VideoProxyService` gained two overridable seams (`httpGetHeaders`, `openRemoteStream`) that the test's anonymous subclass stubs with fixed 200 headers + a 100-byte `php://temp` stream; the subclass overrides `UrlSecurityService::validateVideoUrl` to always pass. Also fixed a latent bug the new test exposed: `StreamedResponse::header()` → `$response->headers->set('Content-Length', ...)` (was a `BadMethodCallException` on any real full/range response).
- [x] **Tests** — 4 new backend tests: `RateLimiterTest` (`room_create_limits_at_5_rooms_per_minute`, `room_create_limiter_is_scoped_per_user`, plus store-route throttle assertion), `RoomManagementTest` (`room_cap_counts_only_recently_active_rooms` — stale rooms outside the 2h window don't count; `store_creates_room_with_owner_as_online_member`). **Results:** `php artisan test` **220/220 passed** (216 baseline + 4 new); `./vendor/bin/pint` clean; no frontend impact.

### Frontend UX Fixes (2026-08-02)
- [x] **Guest video controls** — volume slider, mute toggle, and fullscreen button are now available to every viewer (not just the host). They are local-only and never touch playback state, so sync is unaffected; host-only synced controls (play/pause, skip, seek) stay gated behind `canControl` in `VideoPlayer`. Backend contracts unchanged.
- [x] **Touch-friendly control bar** — the `VideoPlayer` overlay was `opacity-0 group-hover:opacity-100`, unreachable on touch. It is now always visible on coarse-pointer devices (Tailwind `pointer-fine:` variant), with hover/focus-within reveal on fine-pointer devices.
- [x] **Header "اتاق جدید" fixed** — `AppLayout` was posting `rooms.store` with no `name` (always 422). It now navigates to the Dashboard with a `sessionStorage` intent flag (`CREATE_ROOM_INTENT_KEY` in `lib/utils.ts`); `Dashboard` auto-opens the create form on mount.
- [x] **Join error feedback** — the Dashboard join form now renders the server's `errors.invite_code` inline with `role="alert"` (invalid code, full room, locked room, already-a-member). `PageProps` type gained an `errors` field.
- [x] **Friendly full/locked join UX** — `RoomController::join` catches the `AuthorizationException` from `RoomPolicy::join` and redirects back with `withErrors(['invite_code' => …])` instead of a bare 403 page (full: "این اتاق پر است.", locked: "این اتاق قفل شده است.").
- [x] **Set-video error toast** — `Show.tsx` Set-video `catch` no longer silently swallows errors; it fires `toast.error`.
- [x] **Tests** — 3 new `VideoPlayer` tests (guest sees volume/mute/fullscreen, mute toggle, volume→0 mutes) and 4 new `Dashboard` tests (empty state, join-error display, create-form intent open, no intent flag) → frontend 124 → **131**. 4 backend join tests updated from `assertForbidden` to redirect + `assertSessionHasErrors('invite_code')`: `RoomManagementTest` (`full_room_rejects_new_members`, `locked_room_prevents_new_members_from_joining`, `join_race_guard_prevents_overfilling_room`) and `SecurityTest` (`locked_room_prevents_stranger_join`). **Results:** `npm run test` **131/131**; `php artisan test` **220/220** (749 assertions); `npm run lint` 0 errors / 0 warnings; `tsc` clean; Prettier clean; Pint clean.

### Phase 9 — Reliability & Maintainability (2026-08-02)
- [x] **Dead code removed** — `usePollingReload` hook was zero consumers (production polling uses the axios `api` client); deleted `resources/js/Hooks/use-polling-reload.ts` and its PROJECT.md tree entry.
- [x] **VideoPlayer loading/error states** — `VideoPlayer` now destructures `loading`/`error` from `usePlaybackSync`. No-video-yet state shows a spinner while loading ("در حال بارگذاری ویدیو...") and the error message on fetch failure ("خطا در دریافت اطلاعات ویدیو"). When a video IS present but polling fails, a sync-error banner appears ("خطا در همگام‌سازی — در حال تلاش مجدد").
- [x] **room-chat silent send/delete failures** — `sendMessage` and `deleteMessage` catch blocks now fire `toast.error` ("خطا در ارسال پیام" / "خطا در حذف پیام") instead of silently swallowing failures. Polling fetch catch stays silent (it retries naturally).
- [x] **Presence heartbeat lost-update race** — `usePresence` replaced the boolean `cancelledRef` with a `generationRef` counter. Each `scheduleHeartbeat` call increments it; each tick closure captures its own generation and only schedules the next timeout if its generation still matches. This prevents concurrent heartbeat chains when visibility-change events fire during an in-flight API call.
- [x] **Subtitle destroy ordered correctly** — `SubtitleController::destroy` now deletes the DB row first, then the file. The previous order (file, then DB) could leave an orphaned DB record pointing to a nonexistent file if the DB write failed.
- [x] **Tests** — 3 new `VideoPlayer` tests (loading skeleton, error-without-video, sync-error banner with video) → frontend 131 → **134**. `php artisan test` stays at **220/220** (749 assertions — subtitle destroy reorder doesn't change assertion count). **Results:** `npm run test` **134/134**; `php artisan test` **220/220**; `lint` 0 errors / 1 warning (legitimate react-hooks disable in usePresence hook cleanup — Compiler skips micro-optimization on this hook, acceptable trade-off); `tsc` clean; Prettier clean; Pint clean.
- [x] **Audited, intentionally left unchanged** — `VideoStreamTest` (Phase 8 already made the 206 test deterministic; remaining tests are reflection-based or short-circuit before network I/O); subtitle store `localStorage` catch (malformed data → defaults); Show.tsx subtitle-poll catches (already use `setSubError`); member-list/room-settings catches (already use `toast.error`); playback/presence polling catches (silent retry is designed behavior); `usePlaybackSync` has no equivalent race (uses `setInterval`, not recursive `setTimeout`); `docs/SYSTEM.md` historical notes on `usePollingReload` kept (already document it as unused).
- [x] **Deferred scope** — Subtitle file storage directory is cleaned up by `DeleteRoomAction` on room deletion (`Storage::disk('public')->deleteDirectory('subtitles/' . $room->id)`), so a single-track file-orphan on destroy-fail is harmless and doesn't warrant a dedicated cleanup mechanism. Network-level playback fetch retry logic stays unchanged (polling handshake is working).

### Phase 11 — Invite-to-Watch Onboarding (2026-08-02)
- [x] **Registration carries the invite deep link** — `RegisteredUserController::store` now uses `redirect()->intended(route('dashboard'))` instead of a hard `redirect(route('dashboard'))`. A guest who reaches `/rooms/join/{code}`, is redirected to login, and clicks register instead, no longer loses the invite context: after registration the `url.intended` join URL is preserved, so the `verified` middleware chain lands the user back on the join URL post-verification. (The login path already preserved intended via Laravel's `Redirect::guest()` + `redirect()->intended()`.)
- [x] **Email-verification gating audited — NOT actually enforced (corrected 2026-08-04, task 4.4)** — `User` does not implement `MustVerifyEmail` (commented out in `app/Models/User.php`), so the `verified` middleware on room routes is a **no-op** (`EnsureEmailIsVerified` only blocks users that `instanceof MustVerifyEmail`). No verification emails are sent (`MAIL_MAILER=log`), and `VerifyEmailController` would only act on a model implementing the contract. **Email verification is therefore explicitly deferred/not enforced for MVP.** The room routes still carry `verified` middleware (harmless no-op) and the verify-email routes/controllers remain in place so enabling it later means uncommenting `MustVerifyEmail` + configuring a real mailer — a docs/code-consistency correction, not a security regression (the abuse-mitigation claim previously attached to `verified` does not hold in the current code).
- [x] **Deep-link invite copy** — room page header now has "کپی لینک دعوت" (copies the full `route('rooms.join', code)` URL) alongside "کپی کد دعوت"; the room-settings dialog gained a "کپی لینک" button next to code-copy. Guests can now be sent a link instead of a bare code.
- [x] **Join box accepts code or full URL** — new `extractInviteCode()` in `lib/utils.ts` parses a bare code, a relative `/rooms/join/{code}` path, or a full `https://…/rooms/join/{code}` URL; the Dashboard join form uses it and the placeholder now reads "کد یا لینک دعوت را وارد کنید".
- [x] **Misleading YouTube claim removed** — the set-video placeholder advertised "(YouTube, MP4, ...)" but no YouTube/embed playback exists (plain HTML5 `<video>` only). Changed to "آدرس مستقیم ویدیو (MP4, WebM, ...)".
- [x] **First-room onboarding moment** — new `RoomOnboarding` component shown to the owner of a new/empty room (no video set, ≤1 member): three setup steps (add video → share invite → watch together) with "افزودن ویدیو" and "کپی لینک دعوت" actions and a dismiss control. Re-shown per room.
- [x] **Tests** — backend +1 (`RegistrationTest::test_registration_preserves_intended_invite_url`) → **221**; frontend +10 (4 `utils.test.ts` extractInviteCode, 2 `dashboard.test.tsx` join URL/bare code, 4 `room-onboarding.test.tsx`) → **144**. **Results:** `php artisan test` **221/221**; `npm run test` **144/144**; E2E **12/12**; a11y **11/11**; lint 0 errors / 1 warning (pre-existing use-presence hook disable); `tsc` clean; Prettier clean; Pint clean.
- [x] **Deferred scope** — YouTube iframe playback, public rooms, WebSockets, i18n, and admin tooling remain deferred per Phase 10; no scope was added beyond the four Phase 11 priorities.

### Phase 13 — Subtitle Differentiation (2026-08-03)
- [x] **Room-default subtitle track** — nullable `active_subtitle_track_id` FK on `rooms` → `subtitle_tracks` (`nullOnDelete`). Owner-only `POST /subtitles/{room}/default` (`track_id` int|nullable, 404 if track not in this room) and member-visible `GET /subtitles/{room}/default` (`{ default_track_id }`). New `SubtitleDefaultChanged` broadcast event (`subtitle.default.changed` on the room presence channel) mirrors the `PlaybackStateChanged` pattern; both default routes declared before `/{track}` in `routes/web.php`.
- [x] **Guests can browse and select subtitle tracks** — the subtitle manager panel is now shared by all room members (previously owner-only): any member lists tracks and selects one or "بدون زیرنویس". Host-only controls inside the manager: upload file, per-track delete, and a star button that sets/clears the room default with a `(پیشفرض)` label. Video set/change controls remain `isOwner`-gated.
- [x] **Default inheritance + per-user override** — `lib/subtitle-selection.ts` persists the per-user choice in `localStorage` (`tamasharoom-active-track-{roomId}`). No stored choice → inherit the room default (`room.active_subtitle_track_id` shipped via Inertia); stored `number`/`null` → explicit override. A "پیشفرض اتاق" action clears the stored choice to resume inheriting. Deleting the default track clears it server-side.
- [x] **Local time-offset control** — `SubtitleSettings.offset` (ms, −5000..5000, step 250, default 0) added to the store/type; overlay cue timing is now `video.currentTime * 1000 + offset`; settings dialog gained a همزمانی زیرنویس slider with `بدون تأخیر`/`±X.X ثانیه` display and a reset button.
- [x] **Tests** — backend +8 (`SubtitleTest`: owner set/clear default, member/stranger forbidden, cross-room track 404, member read, stranger read forbidden, delete-default clears) → **229**; frontend +12 (2 `subtitle.test.ts` offset, 2 `subtitle-overlay.test.tsx` offset application, 3 `subtitle-settings.test.tsx` offset slider/display/reset, 5 new `subtitle-selection.test.ts`) → **156**. E2E `subtitle.spec.ts` gained a set/read/clear-default flow covering the owner set, member read (distinct forced-new member), member-set 403, and delete-clears-default → **13** total. `routes/test-helpers.php` `join-room` gained a backward-compatible `force_new` flag so E2E can authenticate a genuinely distinct member. **Results:** `php artisan test` **229/229**; `npm run test` **156/156**; E2E **13/13** (verified 2026-08-03 with `APP_ENV=local`); lint 0 errors / 1 warning (pre-existing use-presence hook disable); `tsc` clean; Prettier clean; Pint clean.
- [x] **Deferred scope** — real-time live update of default/selection changes stays on the polling/broadcast path (no live transport yet); per-language track auto-detection and re-encoding remain out of scope.

### Phase 14 — Session Moments & Replay Loop (2026-08-03)
- [x] **O4 — Join/Leave presence moments** — lightweight system-style messages in chat when members meaningfully join or leave (`سارا به اتاق پیوست` / `علی از اتاق خارج شد`). Purely client-side: `lib/presence-moments.ts` exposes a pure `derivePresenceMoments(prev, next)` that diffs consecutive snapshots from the existing 5s presence poll. First snapshot = baseline (no initial-load messages); only `offline→online`, `absent→online`, `online→offline`, and online-vanished transitions emit; `online↔away` never does. `usePresence` keeps a baseline ref + moment id counter and returns `moments`; a failed poll leaves the baseline untouched so retries/visibility refetches can't duplicate. `RoomChat` renders them as centered muted system rows interleaved by timestamp — visually distinct, no avatar/bubble/delete, never counted as unread.
- [x] **O6 — Watch again / session-end moment** — when the current video ends, `VideoPlayer` sets a local `ended` state from the DOM `onEnded` (fires once; repeated ended events / polling re-renders don't duplicate the UI). A centered card shows `دوباره ببینیم` and `پیشنهاد بعدی`. Replay is host-only (button rendered only with `canControl`, and `syncImmediate`/`sync` already no-op for non-hosts): it calls the existing `syncImmediate({ isPlaying: true, positionSeconds: 0 })` → `PATCH /playback/{room}` (authoritative, versioned) and clears `ended`. `onPlay`/replay clear the end UI when playback resumes; a sync-effect guard prevents force-replaying an ended video, and replay re-seeks via the authoritative position-0 signal so guests follow. `پیشنهاد بعدی` uses the existing chat mechanism via `Hooks/use-suggest-next.ts` (posts through `ChatController::store`, toast on failure) — no recommendations engine.
- [x] **O10 — Dashboard re-watch shortcut** — implemented cleanly: `video_url` was already serialized to the Dashboard; added it to the page's `Room` type and render a small non-interactive `دوباره ببینیم` pill when present. The whole card is already a `<Link>` to the room, so the affordance navigates via the existing route with no nested-interactive a11y problem, and normal playback/sync handles the video.
- [x] **Tests** — frontend +34 → **190**: `presence-moments.test.ts` (11 unit: initial no-op, join/leave/away/vanished/unchanged/dedupe), `use-presence.test.tsx` (7 hook: baseline reset, join/leave, dedupe, away no-op, retry-no-dup), `room-chat.test.tsx` (+4: join/leave rendering, no delete, interleave), `video-player.test.tsx` (+8: end card, host replay via syncImmediate, guest no-control, suggest callback, no duplicate, resume clears), `use-suggest-next.test.ts` (2: posts via chat, error toast), `dashboard.test.tsx` (+2: badge only with video). E2E +1 → **14**: `presence-moments.spec.ts` host sees join + leave system messages when a guest joins and leaves. Backend unchanged → **229**. **Results:** `php artisan test` **229/229**; `npm run test` **190/190**; E2E **14/14**; a11y **11/11**; lint 0 errors / 1 warning (pre-existing use-presence hook disable); `tsc` clean; Prettier clean; Pint clean.
- [x] **Deferred scope** — realtime transport (Reverb) for presence moments, media discovery/recommendations, playlists/watch-history, and server-authoritative session-end remain out of scope; the end-card appears on each client via the DOM `ended` event rather than a synced room-wide flag.

### Audit-Fix Pass Phases 0–7 (2026-08-04) — two independent read-only audits, fixed incrementally

- [x] **Phase 0 — verification** — 0.1 confirmed: `streamChunks()` called `set_time_limit(35)` once before the relay loop (long streams truncated; if `set_time_limit` is disabled, ini `max_execution_time` applies). 0.2 confirmed: unauthorized room access returned 403 (`RoomPolicy::view` bool → `authorize` threw), contradicting docs/skills (404 required). 0.3 confirmed: `RoomController::show` eager-loaded the entire chat history (unbounded vs the polling endpoint's 50). 0.4 confirmed: `sendBeacon('/presence/{room}/leave')` sent no body/headers/token and passed CSRF only because browsers send `Sec-Fetch-Site: same-origin` (fragile, transport-dependent).
- [x] **Phase 1 — P1 product bugs** — 1.1 guest playback deadlock: `SecurityHeadersMiddleware` `Permissions-Policy` changed `autoplay=()` → `autoplay=(self)`; `VideoPlayer` gained a viewer-local `شروع پخش` tap-to-play overlay for non-owners (`autoplayBlocked` state, `handleLocalPlay` plays locally without sync), surfacing `video.play()` rejections instead of swallowing them. 1.2 proxy timeout: `streamChunks()` now calls `resetTimeLimit()` (`set_time_limit(0)` where allowed) inside the per-chunk relay loop; deployment-checklist documents `max_execution_time` must be raised/checked on cPanel. Regression: `test_time_limit_is_reset_on_every_relayed_chunk`. Backend → **230**.
- [x] **Phase 2 — security & reliability** — 2.1 room existence disclosure fixed: `RoomPolicy` `view`/`memberAccess` → `Response::denyAsNotFound()` (404 for strangers); owner-only methods (update/delete/kick/transfer/setVideo) → member 403 / stranger 404; `ChatMessagePolicy` `create`/`viewAny`/`delete` aligned; all stranger-assertion tests changed `assertForbidden`→`assertNotFound`. 2.2 subtitles moved from `public` to `local` disk (`SUBTITLE_DISK='local'`); `store`/`index` responses whitelisted (no `file_path`); `DeleteRoomAction` + test-helpers + ProfileTest use `local`; 2 new tests. 2.3 proxy Range fixes: suffix ranges (`bytes=-N` = last N bytes), inverted → 416, upstream-200 → fall back to full-stream 200 (via `lastStreamStatus`), `Accept-Ranges` only when origin supports it; 4 new tests. 2.4 CSP nonce wired before view render so Ziggy `@routes` gets the nonce (`@routes(nonce: $cspNonce ?? null)`); new `ziggy_routes_script_uses_the_csp_nonce_in_strict_mode` test. 2.5 chat delete scoped to `$room->chatMessages()->whereKey()`; new cross-room delete test. 2.6 `.env.example` `BROADCAST_CONNECTION=null`; PROJECT.md production sample + deployment-checklist document `null` + `LOG_CHANNEL=daily`. 2.7 playback drift uses client-monotonic baseline: `PlaybackState.receivedAt` added; `computeExpectedPosition` extrapolates from it; hook sets it on poll receipt and PATCH; clock-offset regression test. 2.8 PATCH ordering guard: only apply when `data.state_version > versionRef.current`; new test. 2.9 E2E `webServer.env.PHP_CLI_SERVER_WORKERS=4` (e2e + a11y configs). Backend → **238**, frontend → **194**.
- [x] **Phase 3 — frontend/a11y/UX** — 3.1 hover-only owner controls (member-list kick/transfer, chat delete) always visible with 8×8 targets + aria-labels. 3.2 seek bar → full `role="slider"` (tabIndex, aria-valuemin/max/now/text, ArrowRight/Left/Home/End, `py-3 -my-3` target); 2 tests. 3.3 chat `pollError` → `role="status"` "در حال اتصال مجدد..." indicator. 3.4 subtitle list distinguishes error vs "هنوز زیرنویسی آپلود نشده است". 3.5 presence status gets `sr-only` text label. 3.6 Dashboard cards restructured (stretched `<Link>` overlay + sibling buttons, no nested interactive). 3.7 `usePresence({ onRemoved })` + `isRemovalError` (403/404) → Show.tsx toasts "شما از اتاق حذف شده‌اید." and redirects to dashboard; 5 tests. 3.8 leave beacon sent from hook cleanup on SPA nav too (guarded `sendLeaveBeacon`). 3.9 heartbeat is the single source of `connected` (poll no longer toggles it). 3.10 new `Components/ui/error-boundary.tsx` wraps `<App>`. 3.11 subtitle delete via `ConfirmDialog` (destructive, loading). 3.12 dashboard delete via `ConfirmDialog` + loading/error (replaces native `confirm()`). 3.13 join converted to POST confirm flow: GET `/rooms/join/{code}` → `joinConfirm` (new `Pages/Rooms/Join.tsx`), POST → `join` (`rooms.join.submit`); Dashboard posts; 2 new RoomManagementTest tests. 3.14 unread tracked by last-seen id (`lastSeenIdRef`) not array-length delta; 2 tests. 3.15 clipboard copy → `safeCopyToClipboard` + success/error toast. 3.16 RTL: `dialog.tsx sm:text-right`→`sm:text-end`, `AuthenticatedLayout space-x-8`→`rtl:space-x-reverse`. 3.17 set-video loading (`Button loading` prop) + success toast. 3.18 subtitle orchestration extracted to new `Hooks/use-subtitles.ts`. Frontend → **201**.
- [x] **Phase 4 — backend correctness** — 4.1 `RoomController::show` chat history bounded to latest 50 + chronological (matches `ChatController::index`). 4.2 leave beacon now sends `_token` via FormData body (CSRF-compatible). 4.3 `DeleteRoomAction` eager-loads `subtitleTracks` (both paths). 4.4 email verification documented as NOT enforced (`User` doesn't implement `MustVerifyEmail`, so `verified` middleware is a no-op; `MAIL_MAILER=log`); TASK.md corrected. 4.5 `UpdatePlaybackRequest` bounds `position/duration_seconds` to `max:86400`. 4.6 `fetchState()` also guards on `documentHiddenRef` so a scheduled poll doesn't fire after tab hides. Backend → **240** (2 new playback-bound tests), frontend → **203**.
- [x] **Phase 5 — documentation reconciliation** — 5.1 migration count 13→14 everywhere (TASK.md, PROJECT_BASELINE). 5.2 authorization contract verified already-consistent (404). 5.3 PROJECT_BASELINE marked historical with prominent banner (counts stale vs TASK.md). 5.4 `SubtitleDefaultChanged` added to FRONTEND_CONTRACT §7.11 + ARCHITECTURE.md. 5.5 `usePollingReload` references removed/corrected (FRONTEND_CONTRACT, ARCHITECTURE, SYSTEM.md). 5.6 `room-create` limiter added to FRONTEND_CONTRACT §7.5, GUARDRAILS, ARCHITECTURE limiter tables. 5.7 LOG_CHANNEL already reconciled (PROJECT.md documents production `daily` vs `.env.example` local `stack`). 5.8 PROJECT.md directory tree updated (`room-onboarding.tsx`, `presence-moments.ts`, `subtitle-selection.ts`, `bootstrap.ts`, `error-boundary.tsx`, `use-subtitles.ts`). 5.9 SYSTEM.md header fixed (concatenated fields → proper format, date updated). 5.10 AGENTS.md polling described as 3s-active/10s-idle tiered cadence. 5.11 quality-report.md banner confirmed prominent (left as-is).
- [x] **Phase 6 — skills reconciliation** — 6.1 trimmed `ai-efficiency` 202→136, `react-rules` 162→148 (removed duplicated `cn()` example + checklist redundancy); code-review/testing-strategy/laravel-backend-rules at 139–142 are dense and at/near the ~130 ceiling. 6.2 `Model::preventLazyLoading(! app()->isProduction())` now actually enabled in `AppServiceProvider::boot()` (matches the skill's claim); fixed the resulting N+1 in `RoomController::show` (set `room` relation on members). 6.3 `skill-maintenance` test-count pointer fixed to `docs/TASK.md` (was `testing-strategy`). 6.4 debugging skill presence line refs 15/16/17→21/22/23. 6.5 debugging `LOG_CHANNEL=daily` claim verified correct (production). 6.6 Zod boundary validation implemented (option a): new `lib/validation.ts` (chat messages, subtitle settings, subtitle tracks) applied at the real API/localStorage boundaries in `room-chat.tsx`, `use-subtitles.ts`, `stores/subtitle.ts`; redundant Dashboard `as` cast removed. 6.7 `room-create` limiter added to security-rules skill. 6.8 performance-rules polling figure → 3s/10s tiered; react-window suggestion removed (not installed, lists bounded). 6.9 output-conventions comment example → 3s/10s. 6.10 laravel-backend-rules now documents 3s/10s tier + `BROADCAST_CONNECTION=null`.
- [x] **Phase 7 — regression tests** — proxy suffix/inverted/origin-ignores-Range + time-limit reset (VideoStreamTest), playback upper-bound (2 new), bounded initial chat history + chronological order (2 new RoomManagementTest), chat pollError reconnecting indicator + recovery (2 new room-chat), leave beacon CSRF body (1 new use-presence), Zod validation schemas (validation.test.ts), keyboard-a11y E2E (`keyboard-a11y.spec.ts`: Tab reaches kick/transfer buttons after opening اعضا tab). **Results:** `php artisan test` **244/244**; `npm run test` **213/213**; E2E **15/15**; a11y **11/11**; lint 0 errors / 1 warning (pre-existing use-presence hook disable); `tsc` clean; Prettier clean; Pint clean; `npm run build` clean.
- [x] **Deferred scope** — none of the audit items deferred; conservative choices flagged in the report (email verification documented as not enforced; join confirm wording; 86400s playback bound). Nothing committed/pushed — the user reviews and commits.

### Verification Pass (2026-08-04) — re-verify audit claims, fix one E2E flake

- [x] **ITEM 1 — Sec-Fetch-Site/CSRF leave-beacon claim ACCURATE** — `Sec-Fetch-Site: same-origin` short-circuits CSRF in real Laravel framework code (`vendor/.../Middleware/PreventRequestForgery.php:145-147`; `ValidateCsrfToken` is a deprecated alias; `Middleware.php:490/625-627` registers it in web). The leave route (`routes/web.php:67`) is CSRF-protected; the beacon already sends `_token` via FormData (`use-presence.ts:220-231`). Live curl proof: no token + SFS same-origin → 200; no token, no SFS → 419; token in body → 200. Added real-CSRF regression test `leave_beacon_requires_valid_csrf_token_in_body` (`tests/Feature/PresenceTest.php`, flips env to local so `runningUnitTests()` doesn't short-circuit). Backend → **245**.
- [x] **ITEM 2 — tap-to-play (P1-01) real-browser proof** — headless Chromium AND system Chrome both exempt loopback origins from autoplay policy (every flag + CDP method ineffective, verified via Win32_Process command line), so the browser block is simulated deterministically with an `addInitScript` that rejects `play()` with `NotAllowedError` until the first click, exercising the real app path (`video-player.tsx` `autoplayBlocked`/`شروع پخش` overlay/`handleLocalPlay`). New `tests/e2e/tap-to-play.spec.ts` (2 tests: overlay → click → real local playback with zero guest PATCH/POST/DELETE mutations; overlay reappears for a fresh guest). Real-media fixture `public/videos/sample.mp4` served same-origin via the new `local_video=1` test-helper branch (direct mode bypasses SSRF). E2E → **17**.
- [x] **ITEM 3 — CSP smoke test under production-like config DONE** — `SecurityHeadersMiddleware` relaxes CSP (adds `'unsafe-inline'`) only on the `local` branch; every non-local env — including `APP_ENV=testing` (phpunit.xml), the same branch production takes — serves the strict nonce-only `script-src 'self' 'nonce-…'`. So the CSP smoke test runs the production-equivalent policy without changing the suite default env. `ziggy_routes_script_uses_the_csp_nonce_in_strict_mode` (`tests/Feature/SecurityTest.php:64-91`) was hardened with an explicit assertion that `script-src` contains no `unsafe-inline` (now 9 assertions), proving any drift to the relaxed policy fails loudly. Realness proven by revert/restore: temporarily reverting `app.blade.php:13` to `@routes()` (no nonce) → test **FAILS** (HTML shows `<script type="text/javascript">const Ziggy=…` with no nonce); restoring `@routes(nonce: $cspNonce ?? null)` → **PASS**. Backend stays **245/245** (842 assertions).
- [x] **`presence-moments.spec.ts` flake fixed** — detection logic (`lib/presence-moments.ts:78-88` in-place offline-from-online + `:91-100` vanished-from-list) was already correct and is covered by direct unit tests (`presence-moments.test.ts:51-70`) plus a live E2E instrumentation run that derived the leave moment. Root cause was the TEST: its `.first()` join assertion matched the owner's spurious reconnect join moment before the host ever polled the guest as online, so the leave was sent while the guest was still unseen-online → no online→offline transition → no leave moment. The test now gates the leave on the guest's *named* join moment (`waitForGuestOnline` reads the guest `user_id`/name from the presence API, then waits for `getByText('${guestName} به اتاق پیوست')`), which guarantees the host's baseline captured the guest online. Verified 3× headed in isolation + full suite. **Results:** `php artisan test` **245/245** (PresenceTest 13/13); `npm run test` **213/213**; E2E **17/17**; lint 0 errors / 1 warning (pre-existing use-presence hook disable); `tsc` clean; Pint clean. Nothing committed/pushed.
- [x] **Fixture staging (ITEM A) settled** — `public/videos/sample.mp4` (1,128,375 bytes / ~1.1 MB, blob `bd91051d…`) staged normally with git (no LFS in this repo; size far under the LFS threshold, so no `.gitattributes` entry added). Fresh-clone proof: `git checkout-index` materialized the exact bytes from the index (SHA-256 `0CD83D…2451` identical before/after), and the tap-to-play E2E then passed **2/2** against the materialized fixture.
- [x] **E2E flake: `subtitle.spec.ts` "Delete subtitle track" intermittent `resp.ok()` false on `POST /__test/setup-verified-room`** — ROOT CAUSE: test helpers created users via `User::factory()` → faker `unique()->safeEmail()`, whose "unique" state lives only for the current PHP process. Under `php artisan serve` the app reboots per request (unique set resets), while the shared dev/E2E SQLite DB accumulates users across runs (601 by the time of investigation). Measured proof: of 500 fresh `safeEmail()` values, 1 (`bbuckridge@example.com`) already existed in the DB → `users.email` UNIQUE constraint → 500 → `resp.ok()` false. Later tests in a run (14th) are likelier to hit it; isolation passes because few users are created. NOT the `room-create` rate limiter — `__test/*` routes carry only the `web` group (no throttle), are CSRF-excluded for `__test/*`, and are gated to `local`/`testing` in `bootstrap/app.php:27-30` (gate correct). FIX: `routes/test-helpers.php` now creates helper users with `test_helper_user_email()` = `'e2e-'.Str::random(12).'@example.com'` (collision-proof regardless of DB size or process boundaries). REGRESSION: `tests/Feature/TestHelperEmailUniquenessTest.php` (2 tests, 1053 assertions) — generator never collides + format pins the `e2e-` scheme (revert-proof: temporarily restoring `safeEmail()` failed both tests deterministically on the format assertion). Verified: full backend **247/247**; 3 consecutive full-suite E2E runs **17/17** under the same sequential single-worker conditions. No temp logging left. Nothing committed/pushed.

### Bug-Fix Batch (2026-08-04) — chat tab-switch loss + dark-mode two-click

- [x] **BUG 1 — chat messages disappear on Chat/Members tab switch** — ROOT CAUSE: `resources/js/Pages/Rooms/Show.tsx` rendered the tab panels with conditional mounting (`{activeTab === "chat" ? <RoomChat/> : <MemberList/>}`), so switching to Members fully unmounted `RoomChat` and threw away its component-local `useState` message list (`room-chat.tsx:35`, seeded from the stale Inertia page-load `initialMessages` snapshot). On remount the list came back stale/empty and only refilled on the next 3s poll — or never, on a poll error race. FIX: both panels stay mounted and the switch is now CSS-only (`hidden`/`h-full` classes on wrapper divs), so `RoomChat` state survives tab switches and no refetch is needed. REGRESSION: new E2E `tests/e2e/chat.spec.ts:101` "Chat messages persist across the Chat/Members tab switch" — fails on the buggy code (1.5s sub-poll timeout: message never visible), passes on the fix; `chat.spec.ts` 3/3 + `keyboard-a11y.spec.ts` 1/1.
- [x] **BUG 2 — dark-mode toggle needs two clicks on first use** — ROOT CAUSE: the theme store defaults to `dark: true` (`resources/js/stores/theme.ts` `getInitialDark`), but nothing ever applied the `dark` class to `<html>` on load — the only `classList` mutation in the frontend was inside `toggle`, and the unused `$themeClass` on `<body>` (`app.blade.php:18`) is never assigned. First click flipped the store to `light` and ran `classList.toggle("dark", false)` — a no-op because the class was absent → no visible change; second click added it. FIX: the store now applies `document.documentElement.classList.toggle("dark", …)` from the initialized state on module load (guarded for SSR), so store state and DOM class agree from first paint and every toggle is visible. REGRESSION: new `resources/js/__tests__/theme-init.test.ts` (4 tests) using `vi.resetModules()` + dynamic import to simulate a fresh load (no stored preference → dark class applied; stored light → not applied; stored dark → applied; single toggle from default state is immediately visible). Revert-proof: with the init line removed, **3/4** of the new tests fail; with it, all pass. **Follow-up (surfaced by the fix):** dark rendering is now the real default, which exposed two genuine dark-mode WCAG AA contrast violations that were previously masked by light-mode renders — landing-page pill `bg-primary/10 text-muted-foreground` (4.32:1) and the room tab bar's inactive tab `text-muted-foreground` on `bg-secondary` (4.19:1). Fixed by raising dark-mode `--muted-foreground` from `40, 5%, 60%` to `40, 5%, 63%` (`resources/css/app.css:76`) → 4.71:1 and 4.57:1. a11y suite back to **11/11**.
- [x] **Pre-existing infra regression fixed** — `npm run build`/`npm run type-check` were failing at `vitest.config.ts:6` (TS2769) because an accidental nested `node_modules/vitest/node_modules/vite@7.3.6` had been installed during the session, conflicting with root `vite@5.4.21` (vitest's declared range `^5.0.0 || ^6.0.0 || ^7.0.0-0` is satisfied by 5.4.21, so the nested copy was wrong). `package-lock.json` (570 insertions) was restored to HEAD and the nested vite/@esbuild dirs removed → `npm ls vite` shows vitest deduped to 5.4.21, `tsc --noEmit` clean, `npm run build` succeeds (8.25s). No lockfile/node_modules change remains in the working tree.
- **Results:** `npm run test` **217/217** (21 files, +4 new theme-init tests); E2E full suite **18/18** (2.1m, includes the new chat regression test and the formerly-flaky subtitle test); `php artisan test` **247/247** (1895 assertions); a11y **11/11**; lint 0 errors / 1 pre-existing warning (< 4 max); `tsc --noEmit` clean; Prettier clean; `npm run build` green. Nothing committed/pushed.

#### Playback-sync E2E verification fixture + Range root cause (2026-08-05)
- [x] **`playback-sync-verification.spec.ts` (2.1–2.4) green 4/4** — the sync E2E suite that failed all session (2.1/2.2/2.4, diff up to 63s) now passes. Long-running `2.2` (host advances 60s while guest backgrounded, restore → within tolerance) and `2.4` (late-join, no host regression/oscillation) pass with real drift 1.6–3.9s.
- [x] **Root cause of the sync E2E failures was NOT the app fix — it was the dev server.** `php artisan serve` (PHP built-in server) does not honor HTTP `Range` requests: it answers 200 with the full body (no 206/`Content-Range`), so Chromium cannot seek partially-loaded media on it at all (verified: even the 5s `sample.mp4` can't seek, `currentTime` snaps to 0). Playback-sync drift correction *is* seeking, so those tests could never converge on the dev server regardless of fixture. Production is cPanel Apache, which supports Range. Fix: the spec installs a test-only `installRangeMock` that intercepts `/videos/*.(webm|mp4)` and serves 206 slices — replicating the production contract without touching app code.
- [x] **The old "sample.mp4 is 109s" belief was wrong.** `git log --follow` shows `sample.mp4` was always the committed 1.1MB / 5.055s file (no corruption); it is untouched.
- [x] **New long fixture `public/videos/sync-sample.webm` (~95s, finite duration, fully seekable)** generated with headless Chromium `canvas.captureStream()` + MediaRecorder (VP8 — MP4/H.264 records only ~4s due to frame dropping). Key tricks discovered: the canvas must be **appended to the DOM** (otherwise `captureStream` emits only ~101 sparse frames and recording caps at ~29s regardless of wall time), and the WebM needs an EBML **`Duration` element + a Cues index** (finite duration makes Chromium byte-estimate seeks, which fail without Cues). Verified: duration 94.96s, 640×360, seeks to 15/50/85 land correctly on a Range-capable server. All scratch generators/probes cleaned up; only the fixture remains.
- [x] **`routes/test-helpers.php` `local_video` branch gained a `video_file` query param** (default `sample.mp4`, so `tap-to-play.spec.ts` is unaffected); the sync spec passes `video_file=sync-sample.webm`.
- [x] **Spec rewritten for dev-server realities**: `waitForVideo()` replaces `waitForLoadState("networkidle")` (polling never lets network idle); `startPlayback()` dispatches the "شروع پخش" overlay click synchronously via `evaluate` (pointer click races React re-renders driven by 3s polls); `DRIFT_TOLERANCE` = 7 (3s poll + 2s threshold ⇒ steady-state lag observed 1.6–6.5s); 2.3 no longer uses `route.fetch()` (it hangs against `php artisan serve` for PATCH) — the held first PATCH is never sent to the server and its response is synthesized with `state_version: 0`, while later PATCHes forward via `route.continue()`.
- **Pre-existing (not caused by this work, verified by reverting helper + running at HEAD):** `tap-to-play.spec.ts` test 1 "autoplay blocked → overlay shown → click starts local playback" fails — the guest's video loads via `/proxy/video/{id}` (VideoPlayer intentionally keeps the proxy path even in direct mode) and playback stalls at `currentTime` 0 after the overlay click on the dev server. Out of scope for the sync-verification batch; flagging for a follow-up.
- **Results:** `playback-sync-verification.spec.ts` **4/4** (fresh build, sequential worker); `room.spec.ts` 3/3; `type-check` clean; `npm run lint` 0 errors / 1 pre-existing warning; Pint clean. Nothing committed/pushed.

#### Follow-up batch (2026-08-05): parts 1–3 consolidation + OpenCode config + test fixes
- [x] **Docs/skills drift checker** — `scripts/check-docs.cjs` (`npm run check:docs`): frontmatter name↔dir, broken path refs, test-count patterns outside TASK.md, refs to deleted skills, debugging-skill `file:line` bounds + polling-timings constant spot-checks. Clean (44 files). Wired into CI after Prettier.
- [x] **Docs/skills hygiene (parts 1–2)** — AGENTS.md Git-safety bullet; `deployment-checklist` `BROADCAST_CONNECTION=null`; TASK.md `## Testing` re-verified; `skill-maintenance` largest-skill claim fixed (code-review-rules ~145); `ai-efficiency` SYSTEM.md line count; `output-conventions` PascalCase Layouts/Pages rows; de-duplicated 6 repeated rules (single-owner + cross-refs); trimmed react-rules 148→136, accessibility-rules 63→50, mvp-scope-decisions 96→95; eslint `no-console` (allow error/warn); `.gitignore` debug-artifact patterns; CI `format:check` + gitleaks steps; no pre-push hook.
- [x] **Observability** — `tools/debug/_harness.cjs` + README (gitignore keeps only README + `_harness.cjs`); `resources/js/lib/debug.ts` (`VITE_DEBUG`-gated, `[debug:<topic>]`, DCE in prod); code-review-rules retirement line; debugging skill `Log::debug('[debug:<topic>]', [...])` convention.
- [x] **AI-gateway cleanup** — deleted `NVIDIA_API_KEY` (user env) and the `zyloo` entry from `~/.local/share/opencode/auth.json`; kept `ROUTER_9_API_KEY`. Gitleaks full-history scan (part 1): **clean**; repo is **public** on GitHub.
- [x] **`opencode.json` simplified to the actual setup** — DeepSeek direct only (`deepseek/deepseek-v4-flash` default + `implementer` agent); `desktop-ai` read-only agent (`edit: deny`, `bash: ask`). OpenRouter/9Router/Zyloo/NVIDIA provider configs removed (not in use; no fake/placeholder providers). No `skills` list — skill discovery stays dynamic via `AGENTS.md`. WORKFLOW.md mapping note updated. Validated against the opencode JSON schema + models.dev.
- [x] **Vitest fix (root cause)** — `video-player.test.tsx` "seek bar is a keyboard-operable slider for the host" relied on the host receiving server drift-corrections to seed the mock video's `currentTime`; the `!canControl` host-gating (host is authoritative, not corrected) removed that, so the host sought relative to 0. Fix: the test now seeds the video's `currentTime` to 10 explicitly (assertion unchanged). Suite back to **217/217**.
- [x] **E2E tap-to-play fix (root cause)** — the guest's `play()` rejected with `NotSupportedError` because its transient src was the SSRF-blocked `/proxy/video/{id}` (returns 400 for the loopback room URL), and both `handleLocalPlay` and the sync effect treated *any* rejection as an autoplay-policy block via blanket `.catch(() => setAutoplayBlocked(true))`, re-showing the overlay and pausing the guest even after the direct URL loaded. Two production fixes in `video-player.tsx`: (1) only `NotAllowedError` re-shows the tap-to-play overlay (media errors fall through to the proxy→direct fallback); (2) the src-effect now loads the **mode-correct URL** (direct URL for direct-mode rooms — no keep-proxy that stranded direct rooms on the proxy). Also hardened the test's advancement check to poll (was a fixed 1.5s window that flakes under one-worker load). `tap-to-play.spec.ts` **2/2**, playback-sync **4/4**, full E2E **22/22**.
- [x] **Stale `BROADCAST_CONNECTION=log` → `null`** — `docs/SYSTEM.md`, `docs/ai/ARCHITECTURE.md` (×2), `docs/ai/PROJECT_BASELINE.md` (all current-state descriptions, not historical).
- **Results (full, 2026-08-05):** `php artisan test` **247/247** (1895 assertions); `npm run test` **217/217**; `npm run test:e2e` **22/22**; `npm run test:a11y` **11/11**; `tsc --noEmit` clean; ESLint 0 errors / 1 pre-existing warning; Prettier `format:check` clean; Pint clean; `npm run check:docs` clean; `npm run build` green; `git diff --check` clean. Nothing committed/pushed.

#### TypeUI selective-integration pilot + Landing Page redesign (2026-08-05)
- [x] **TypeUI MCP integration (minimal)** — `opencode.json` gained a single remote MCP block (`typeui` → `https://mcp.typeui.sh/mcp`, browser OAuth, auth stored outside the repo at `~/.local/share/opencode/mcp-auth.json`). No provider/agent/permission/skill/package.json changes; no credentials in repo; no `.env` edits. CLI verified: `opencode mcp list` → 1 server, `⚠ typeui needs authentication` (expected — config loads at opencode startup, so MCP itself was not exercisable in-session; pilot used TypeUI's public repo skill/prompt content fetched from GitHub instead). DESIGN.md status confirmed (draft → confirmed) and recorded here.
- [x] **Landing page redesigned per TypeUI-guided exploration → Direction 1 "The Dark Living Room"** — `resources/js/Pages/Welcome.tsx` rewritten: full-bleed warm-dark hero (section `bg-foreground text-background dark:bg-card dark:text-card-foreground`), framed "screen" hero visual (همزمان badge, `۲ دوست آنلاین` avatar stack with `-space-x-2 space-x-reverse`, `dir="ltr"` mono invite chip `TR-7K2M`, `aria-hidden` decorative blur orbs), single primary CTA (`شروع کنید — رایگان` / `ورود به داشبورد`) + secondary outline `ورود`, numbered 3-step `<ol>` process with dashed connector replacing the old feature grid, closing warm-dark CTA band, footer with year. RTL-correct throughout (logical properties `ms-*`/`me-*`/`start-*`/`end-*`, `text-start`, RTL icon row). Directions 2 ("Poster Wall" shelf/bento) and 3 ("Cinema Marquee" big type) rejected: they fight DESIGN.md anti-patterns (feature grid, centered layout, gray placeholder boxes) or lose the watch-party storytelling. Landing-page prop contract unchanged (`auth`, `canLogin`, `canRegister`, `laravelVersion`, `phpVersion` — FRONTEND_CONTRACT.md §1.1).
- [x] **Verification** — `npm run lint` 0 errors / 1 pre-existing warning; `npm run type-check` clean; `npm run format:check` clean; `git diff --check` clean; `npm run test` **217/217**; `tests/a11y/welcome-a11y.spec.ts` **1/1** (axe: no critical/serious on `/`). No backend or test files changed; nothing committed/pushed.

#### CI fix — Inertia page path casing (2026-08-05)
- [x] **GitHub Actions PHPUnit failure `Inertia page component file [Rooms/Show] does not exist` (2 tests)** — ROOT CAUSE: no `config/inertia.php` is published, so Inertia's package default page search path applies: `resource_path('js/pages')` — **lowercase `pages`** (`vendor/inertiajs/inertia-laravel/config/inertia.php`). The repo stores page components under `resources/js/Pages` — **uppercase** (git tracks `resources/js/Pages/Rooms/Show.tsx`; exact-case probe over on-disk dirs finds no lowercase `pages`). `AssertableInertia::component()` (`vendor/.../Testing/AssertableInertia.php:102-115`) resolves via `app('inertia.view-finder')->find('Rooms/Show')` → `FileViewFinder::findInPaths` → `files->exists($viewPath)`. On Windows (case-insensitive NTFS) `js/pages/...` matched `js/Pages/...` → local 247/247 green; on ubuntu-latest (case-sensitive ext4) the lowercase path doesn't exist → `InvalidArgumentException` → assertion fails. Only these 2 tests failed because they are the *only* tests asserting `->component('Rooms/Show')` (`RoomManagementTest.php:609,632`).
- [x] **FIX (configuration, no test change)** — published `config/inertia.php` with `pages.paths` = `resource_path('js/Pages')` (exact repo casing; `pages.ensure_pages_exist=false`, extensions `js/jsx/svelte/ts/tsx/vue` unchanged). `inertia.testing.ensure_pages_exist` stays `true` — the page-existence assertion is NOT weakened. `ServiceProvider::mergeConfigFrom` shallow-merges the published file over the package default (verified: `config('inertia.pages.paths')[0]` = `...resources\js/Pages`), and the finder now resolves `Rooms/Show` → `resources/js/Pages/Rooms/Show.tsx`. Exact-case simulation proves the old default would fail on Linux ext4 while the new path resolves. No CI workflow change, no `resources/js/Pages` rename, no imports touched.
- [x] **Verification** — `php artisan test` **247/247** (1895 assertions; the 2 previously-failing `initial_room_load_*` tests pass); `./vendor/bin/pint --test config/inertia.php` clean; `php artisan config:cache`/`config:clear` succeed; `npm run type-check`, `npm run lint` (0 errors / 1 pre-existing warning), `npm run format:check`, `npm run check:docs` all clean. Nothing committed/pushed.

#### CI/build hygiene pass (2026-08-05) — 5 commits pushed, CI green (#61)
- [x] **Vazirmatn font bundled as a Vite asset** — `resources/fonts/vazirmatn-var.woff2` + `resources/css/fonts.css` (`src: url("../fonts/vazirmatn-var.woff2")`) + `resources/views/app.blade.php` preloads via `Vite::asset('resources/fonts/vazirmatn-var.woff2')`. Kills the CI font warning (no public file at `public/fonts/` on CI); `public/fonts/vazirmatn-var.woff2` removed. Verified: no font warning in build, `npm run build` emits `public/build/assets/vazirmatn-var-BeciDpKm.woff2`.
- [x] **React Compiler warning fixed (`use-presence.ts`)** — the `// eslint-disable-next-line react-hooks/exhaustive-deps` at line 169 (suppressing a false positive about reading `generationRef.current` in an effect cleanup) was itself causing the React Compiler to bail out. Replaced by module helper `invalidateGeneration(generation)`; cleanup now calls `invalidateGeneration(generationRef)`. ESLint and Compiler both clean; lint/type-check pass. Remaining intentional directives: `video-player.tsx:66` + `use-toast.ts:54` disable the compiler rule, `lib/debug.ts:44` no-console.
- [x] **Lockfile patched to patched transitive versions** (surgical manual edits, registry-verified integrity, within semver ranges) — `brace-expansion@1.1.16 → 1.1.18`, `@typescript-eslint/typescript-estree/node_modules/brace-expansion@5.0.7 → 5.0.9`, `postcss@8.5.20 → 8.5.25`. `npm audit` dropped **4 → 2** (brace-expansion + postcss resolved). Remaining: `vite` high (direct; 4 advisories: optimized-deps `.map` path traversal, launch-editor NTLMv2 UNC, `server.fs.deny` bypass, esbuild) + `esbuild` moderate — dev-server-only; the only fix is the breaking `vite@8.2.0` upgrade (cascades laravel-vite-plugin 3, plugin-react 6, Tailwind/vitest compat), so it is documented and left. Deprecations (`@babel/plugin-proposal-private-methods` via eslint-plugin-react-compiler, `whatwg-encoding` via jsdom) are non-vulnerabilities, left.
- [x] **CI runtime actions on Node 24** — `actions/checkout@v6`, `actions/setup-node@v6` (`node-version: 24`, `cache: npm`), `gitleaks/gitleaks-action@v3`; kills the Node 20 deprecation warning. `cache: npm` "not found cache" message verified as standard setup-node behavior — no change needed.
- [x] **Gitleaks CI failure root-caused + fixed (commit `0024f31`)** — run #60 failed with `failed to scan Git repository error="stderr is not empty"`. Cause: the gitleaks action computes `baseRef`/`headRef` and scans `--log-opts=baseRef^..headRef` when they differ, but `actions/checkout` default `fetch-depth: 1` omits the base commit → `git log` fails. Only multi-commit pushes trigger it (single-commit pushes scan HEAD). v2/v3 scan logic is identical; gitleaks 8.24.3 is the pinned version. Local proof: full-history scans (8.30.1 repo-wide, 8.24.3 over `8e866b9^..8dd6c79`) clean; a `git clone --depth 1` + range-scan repro exits 1. Fix: `fetch-depth: 0` on the checkout step (comment documents why).
- [x] **5 commits pushed (`1583e94..0024f31`), CI run #61 (`31037849182`) fully green** — `8e866b9` font bundling, `2060b45` React Compiler re-enable, `7a11fc2` lockfile patches, `8dd6c79` Node 24 actions, `0024f31` gitleaks `fetch-depth: 0`. All 27 CI steps success, **zero annotations** (Node 20 deprecation, React Compiler, font, cache all gone). Remaining audit findings documented above and not addressable without breaking majors.
- [x] **Verification (local, pre-push)** — `npm run build` clean; `npm run lint` (0 errors / < 4 max-warnings); `npm run type-check` clean; `npm run format:check` clean; `npm run check:docs` clean (44 files); `npm run test` **217/217**; `npm run test:e2e` **22/22**; `npm run test:a11y` **11/11**; `git diff --check` clean. `php artisan test` skipped locally (no PHP changes; CI covers). Note: local `npm install`/`npm audit fix` remain blocked by npm 12 `EALLOWREMOTE` on `@tailwindcss/oxide-wasm32-wasi` — pre-existing, CI unaffected, do not alter repo `.npmrc`/lockfile to work around it.

### Dashboard UI/UX Redesign (2026-08-05) — "The Dark Living Room" direction
- [x] **Audit** — Dashboard data = `RoomController@index` (`routes/web.php:24`, name `dashboard`) → `Inertia::render('Dashboard', ['rooms' => ...])`; rooms = owned-or-member, eager-loads `owner:id,name`, `withCount('members')`, `latest('last_activity_at')`. Per-room Inertia contract (unchanged): `id`, `name`, `invite_code`, `owner {id,name}`, `members_count`, `max_members`, `is_playing`, `video_url`, `last_activity_at`, `user_id`, `is_locked`. The direct Eloquent payload serializes `is_locked`, which `RoomCardRoom.is_locked` uses for the lock chip.
- [x] **Design direction** — warm-dark cinematic "Dark Living Room" (mirrors the approved Landing redesign): header band (اتاقهای من + "یک اتاق بساز، دوستانت را دعوت کن و با هم فیلم ببین."), a primary action band (create on the `start`/right, join on the `end`/left, split with `lg:border-s`/`lg:ps-8`), room cards kept as compact info rows (not posters — `video_url` is the only artwork and is usually null). Brand stays تماشاروم; no `TamashaRoom` Latin in UI copy. TypeUI MCP was intentionally NOT installed/imported (its hosted prompt resolver requires project-local skill files — the brief forbids new dependencies); direction is a hand-composed design from the existing TamashaRoom design system.
- [x] **`resources/js/Pages/Dashboard.tsx` rewritten** — persistent section headings + toggleable create form (name input + ساخت اتاق, `autoFocus`, انصراف), join form (کد یا لینک دعوت input that accepts paste of a full invite URL via existing `extractInviteCode`, پیوستن به اتاق now waits for `onFinish` instead of redirecting instantly), polished empty state (dashed card, Tv icon, 2 CTAs — "اولین اتاق را بسازید" and "پیوستن با کد دعوت" which only focuses the join input), room grid `sm:grid-cols-2 lg:grid-cols-3`, ConfirmDialog for delete, ToastContainer, `Dashboard.layout = AppLayout`. Intent auto-open via `sessionStorage` (`CREATE_ROOM_INTENT_KEY`) preserved. No backend/contract/route changes; join/create/delete still use the existing `rooms.join.submit/{code}`, `rooms.store`, `rooms.destroy` routes.
- [x] **`resources/js/Components/composite/room-card.tsx` (new)** — shared `RoomCardRoom` type (Dashboard fields, including serialized `is_locked`) and `RoomCard`: stretched-overlay `Link` to the room (`aria-label="ورود به اتاق {name}"`, `focus-visible:ring-2`) with copy/delete as `pointer-events-auto relative z-10` sibling buttons (no nested interactive elements); chips for در حال پخش/متوقف, members `toPersianDigits(n) از toPersianDigits(n) نفر`, قفل when locked, دوباره ببینیم when `video_url`; invite code in a `dir="ltr"` mono chip; delete button `aria-label="حذف اتاق {name}"`.
- [x] **`resources/js/lib/utils.ts`** — `timeAgo` returns Persian digits (`۲ دقیقه پیش`, `۵ ساعت پیش`, `۳ روز پیش`) via `toPersianDigits` — closes a DESIGN.md Persian-digit gap (previously Latin digits). No test depends on the relative-time strings.
- [x] **Behavior preserved** — create/join/delete wiring identical; loading states on submit buttons; error toasts on delete; join errors from Inertia props (`pageErrors.invite_code`) rendered with `role="alert"`; empty state still auto-opens create on intent. Only deliberate changes: persistent section headings (the create form toggles, not the heading), join waits for `onFinish`, redundant sr-only span under the link's `aria-label` removed.
- [x] **Tests** — `resources/js/__tests__/room-card.test.tsx` (new, 7 tests): room name + owner, Persian-digit member count, paused/playing chips, lock chip, دوباره ببینیم toggle, `dir="ltr"` invite chip, stretched-link accessible name, copy + delete actions. `resources/js/__tests__/dashboard.test.tsx` updated to match real behavior changes only: `Head` added to the Inertia mock (page now renders `<Head>`), join assertions expect `{}, { onFinish }` call shape, intent tests assert the form control (`نام اتاق` input) instead of the now-persistent heading, fixtures gained `is_locked`.
- [x] **Verification (local)** — `npm run type-check` clean; `npm run lint` clean (0 errors / < 4 warnings); `npm run format:check` clean; `git diff --check` clean; `npm run check:docs` clean (44 files); `npm run build` clean (Dashboard chunk 12.49 kB / 5.13 kB gzip); `npm run test` **224/224**; `npm run test:e2e` **22/22** (room.spec.ts create/join flows exercise the new Dashboard); `npm run test:a11y` **11/11** including the Dashboard axe audit. Programmatic visual QA (temp spec, since removed): mobile/tablet/desktop → `dir=rtl`, dark-class default, no horizontal overflow (`scrollWidth <= clientWidth`), Persian-digit member text, invite-code chip `dir=ltr`, create-form autofocus, theme toggle → light, visible keyboard focus ring (box-shadow) on the room-card link. Screenshots left at `test-results/visual-qa-*.png` for eyeball review (not committed).
- [x] **Commit + CI** — single focused commit `eff1e30` including this TASK.md update; pushed to `origin master`; GitHub Actions run `31041511207` fully green. No new dependencies; no backend or route changes; nothing outside `Dashboard.tsx`, `room-card.tsx`, `utils.ts`, and the two test files.

#### Deployment Readiness
- [ ] **Migrations on production** — not executed (all 14 migrations have run only on local SQLite)
- [ ] **Queue worker in production** — none needed: queue is drained in batches by the `schedule:run` cron (`queue:work --stop-when-empty`); no persistent worker to start
- [ ] **cPanel cron for `schedule:run`** — not added
- [ ] **`SESSION_SECURE_COOKIE=true`** — in `.env.example` since 2026-07-22; must be confirmed `true` in production `.env`
- [ ] **`APP_DEBUG=false`** — must be confirmed on production; currently `true` in local `.env`
- [ ] **`APP_ENV=production`** — must be set on production; currently `local`

## Pending

### Known pre-existing test issues (not introduced by the 2026-08-13 video-proxy fix)
- [ ] **contrast-a11y.spec.ts strict-locator failures (2)** — `getByRole('button', { name: 'زیرنویس' })` substring-matches both the "زیرنویس" and "تنظیمات زیرنویس" toolbar buttons ("Room open states" + "Room dialogs"). Reproduced identically on the unmodified code; unrelated to the proxy fix. Fix: use `exact: true` (or `.first()`) and confirm intent.
- [ ] **chat.spec.ts tab-switch timeout** — "Chat messages persist across the Chat/Members tab switch" intermittently times out on the 5s `toBeVisible` after send on this 1-CPU machine; passes on re-run. Pre-existing timing flake, unrelated to the proxy fix.

### Deployment
- [ ] Run migrations on production database
- [ ] Configure queue drain for production (confirm `QUEUE_CONNECTION=database`; queue is processed in batches by the `schedule:run` cron — no worker/supervisor needed)
- [ ] Set up cPanel cron entry for `* * * * * php /home/zizolear/public_html/tamasharoom/artisan schedule:run >> /dev/null 2>&1`
- [ ] Confirm `APP_ENV=production`, `APP_DEBUG=false`, `SESSION_SECURE_COOKIE=true` in production `.env`

### Launch Blockers (single-core budget — SYSTEM.md 21.10)
- [x] **Enforce per-room member cap at join time**, including a `lockForUpdate()` guard against a join race on the last slot — `JoinRoomRequest` validates invite code; `RoomController@join` uses `DB::transaction` + `lockForUpdate()`; policy checks `isFull()` inside the locked transaction
- [x] **Enforce a system-wide active-room cap** — `config/tamasharoom.php` (`max_concurrent_rooms`, default 50); `StoreRoomRequest::after()` checks active room count on creation; user-facing validation error on reject
- [x] **Extend `rooms:prune-inactive`** to delete subtitle files from disk, subtitle tracks, chat messages, and members alongside the room row — uses `DeleteRoomAction` with `chunkById` for memory-safe pruning
- [x] **Ensure the owner-initiated "Delete room" path** uses the same cleanup logic — `RoomController@destroy` now uses `DeleteRoomAction`
- [x] **Implement `DetermineVideoPlaybackModeAction`** — SSRF-safe HEAD check; returns `'direct'` when source has CORS `*` + `Accept-Ranges: bytes`; falls back to `'proxy'`; stored on room row; frontend `VideoPlayer` reads `playback_mode` to choose direct or proxy source
- [x] **Add production error monitoring (Sentry)** — `sentry/sentry-laravel` v4.27 installed; `config/sentry.php` published; `.env.example` has `SENTRY_DSN` placeholder; disabled when DSN is empty

### Future Features
- [x] Room ownership transfer UX polish (update member list after transfer) — **fixed 2026-08-01 (Batch 2C, TAM-005)**: ownership state now derives from the reactive `room-ui` store via new `useRoomOwnership` hook; old owner loses owner-only controls immediately, new owner adopts ownership from presence data, failed transfers leave state untouched. See Completed section.
- [ ] Self-hosted WebSocket migration (Laravel Reverb) for post-MVP VPS scaling (>500 concurrent) — Pusher Channels push delivery (primary) with tiered polling fallback was completed 2026-08-07 (DECISION-002a); self-hosted Reverb remains a post-MVP VPS roadmap item.
- [ ] Chat/room moderation — report message, owner can delete any message (not just their own)
- [x] Cover profile, password-reset, verify-email pages with a11y audits — **done 2026-08-01 (Batch 2D, TAM-004)**: axe tests now cover `/reset-password/{token}`, `/confirm-password`, and the Profile delete-account modal open state; expanded to 19 tests across 5 spec files. See Completed section.
- [x] E2E tests for chat, subtitle, lock/kick, and transfer flows (now covered — 22 Playwright E2E tests across 8 spec files)

### Accepted MVP Limitations (tech debt)
- [ ] **SSRF TOCTOU gap (partially closed 2026-08-01)** — `UrlSecurityService::validateVideoUrl()` runs DNS resolution and IP checks once at the top of `VideoProxyService::stream()`. Within a single request, a DNS rebinding attack could pass validation for a safe IP and then resolve to a different (internal) IP by the time the HEAD request runs. The window is microseconds and the proxy requires authentication, so the risk is accepted for MVP. Batch 1 closed the *redirect* half of this: redirects are no longer auto-followed — each hop is re-validated by `UrlSecurityService` (TAM-007) — and TLS verification is now enabled on all stream contexts (TAM-009). Post-MVP fix: resolve the hostname synchronously, compare the resolved IP against the blocklist inside every stream context (as a `stream_context_set_param` wrapper), and fail on mismatch.
